# 2026-07-27 最近 Codex 选模修复收尾

## 未完成项与根因

- 复核最近提交和
  `docs/superpowers/plans/2026-07-24-codex-model-selection-recovery.md`
  后确认：功能代码、记忆提交和远端推送已经完成，但计划中的重启/线上日志验收仍未
  留下完成证据。
- 首轮非慢全套发现真正的本地门禁缺口：
  `test_archived_doc_noise_paths_are_removed` 失败。最近提交把已完成计划重新放进
  仓库明确禁止的 `docs/superpowers/` 归档噪声路径，违反文档瘦身契约；这与记忆中
  提到、但当前单独运行已通过的 Autonomous 雇佣锁序用例无关。

## 修复

- 将计划迁移到现行顶层文档位置
  `docs/2026-07-24-codex-model-selection-recovery-plan.md`，移除空的
  `docs/superpowers/` 目录，并同步修正 7 月 24 日记忆中的引用。
- 将计划中已经由提交 `48256267` 完成的记忆提交步骤标记为完成；部署与线上验收
  步骤只在取得实际重启日志证据后关闭。

## 验证

- RED：非慢全套
  `1 failed, 12424 passed, 9 skipped, 99 deselected, 45 subtests passed`，
  唯一失败为归档文档路径守卫。
- GREEN：`tests/test_docs_references.py` 为 `4 passed`。
- Codex 选模、系统指令门禁、按钮、WS 与启动相关回归为 `205 passed`。
- 修复后非慢全套为
  `12425 passed, 9 skipped, 99 deselected, 45 subtests passed`。
- 改动相关 Ruff、`git diff --check` 和
  `uv run python -m src.main --validate` 均通过。

## 部署验收

- 文档契约修复提交 `00c90c2b` 已在确认远端无领先提交后推送到 `origin/dev`。
- `./restart.sh rr` 于 05:11:46 UTC 触发；日志依次确认 Python 依赖已同步、
  bubblewrap 员工沙箱就绪、Codex ACP fallback `1.1.2` 就绪、start begin、
  PID `96583` 存活、remote worker status 0。
- Codex 后台预热于 05:11:54 成功发现 7 个模型，耗时 4011.4 ms；05:12:15
  应用记录“启动飞书长连接服务”及“正在建立飞书长连接”。新重启标记之后未出现
  `ERROR`、`FATAL` 或 traceback，原计划的重启与线上日志验收已完成。
