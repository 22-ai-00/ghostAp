# 2026-08-10

## 三目标最终收口与验证

### 结果

- 总代码量从基线 413,233 行降至 205,582 行：生产 Python 136,714 行、Workflow JS 1,855 行、测试 67,013 行。
- 共减少 207,651 行，减幅 50.250%，当前总量为基线的 49.750%。
- 普通编程、Deep、Spec、Workflow 均使用全自动主路径：安全推荐自动采用，未授权高风险自动拒绝，恢复与 continuation 有界，显式 stop/cancel 保留。
- Dynamic Workflow 保持 GhostAP 自有 Node sandbox 与 6+2 原语；卡片完整展示 direct-agent 调度、单条当前操作、attempt、阶段、token、耗时和无截断终态账本。

### 本轮关键修复

- Workflow：修复 `_replace_js_quote` 对双引号字符串中 apostrophe 的误改；`verify()` 强制 closed evidence schema；`race()` 钳制超长 timer、合并历史失败，并用 AsyncLocalStorage 阻止 deadline 后延迟派发；stderr 单行 4 KiB、总计 64 KiB。
- Spec：恢复次数写入 checkpoint，并使用跨进程非阻塞 `flock` lease；持锁重读权威状态并将自动恢复限制为一次。
- Project Chat：明确未提交的绑定 Saga 在补偿后立即中止，不再错误 complete/jump/welcome。
- 普通编程：非流式卡片回退不再解引用空 `prog_session`；六后端仍执行 factory -> prompt；ProgrammingCardSession 使用权威 active-text 集合。
- Autonomous：修复 blocked 通知已删方法、Context composition 剪枝残留、Ingress models 孤立 decorator、测试 helper 导入与 hire port 语法错误。
- Card/Feishu：每测使用独立 DeliveryRegistry 生命周期；清除 reducer 对退役 `_RETRY_ACTIONS` 的引用；失败 Agent token/duration 纳入 Workflow 指标；ChatLockGate 卡片投递失败直接降级文本反馈。
- ACP：迁移官方 schema `KillTerminalCommandResponse` 到 `KillTerminalResponse`；修复 sync adapter 缺失 json 与 event-fence monkeypatch 目标。
- Tasking：终态事件不再重复携带最后的 running progress。

### 测试迁移与精简

- 删除仅保活已退役 UnifiedContextStore CRUD/version/serialization、Workflow preview/menu、legacy ingress/gateway、Spec pause/resume/shared-reviewer 等 facade 的测试。
- 保留并迁移 append-only context、租户/聊天隔离、Journal/Effect 锚定、R4 默认拒绝、自动恢复、锁、补偿、任务/current operation/subagent、终态分页和 HTML fallback 等核心合同。
- 最终 inventory：212 个测试文件、2,399 个测试函数、65,349 测试源代码行、0 个精确重复组。

### 最终验证

- `uv run ruff check src tests`：All checks passed。
- `uv run python scripts/test_inventory.py tests/`：通过，0 duplicate groups。
- `uv run python -m pytest tests/ -q -m "not slow"`：2,832 passed，26 deselected，39 subtests passed。
- `uv run python -m pytest tests/ -q`：2,858 passed，39 subtests passed；唯一 warning 为第三方 lark-channel protobuf 的 datetime 弃用提示。
- `uv run python -m src.main --validate`：配置校验通过。

### 剩余环境提示

- 当前配置 `INGRESS_ACCESS_MODE=legacy_allow_all`，校验器明确标记为 time-bounded break-glass 模式；不是本轮代码失败，但生产部署应按计划切回受限 ingress 策略。
- 第三方 `lark-channel-sdk` 内部仍使用已弃用的 `datetime.utcfromtimestamp()`；本仓库测试不失败，待上游升级。

## 服务启动与主 Bot 回复链修复

- 现象：进程和 WebSocket 可启动，但主 Bot 回复因 outbound audit 被错误替换为不可用占位实现而 fail-closed；同时旧 Journal 在 Router/Gateway 投影恢复时触发 schema 错误，restart readiness 仍被过早发布。
- 根因：`EmployeeDepartmentRuntime` 创建并持有 `_owned_main_bot_send_audit`，但属性与 readiness 读取已删除的 `_main_bot_activation_audit`；精简后移除了历史 `control_consumed`、旧 authorized payload 和 `slock_*` binding 的重放兼容；WebSocket connected 被误当成完整回复链健康。
- 修复：统一 outbound audit 单一事实源；仅在 Journal 重放期间精确兼容已知旧事件/字段，主动写入和公开 schema 继续严格拒绝；readiness 增加主 Bot 回复链与审计可用性门禁，并支持恢复后的晚就绪。
- 安全性：未删除、重置或重写现有 Journal；Gateway 兼容仅接受完整三字段 `slock_* -> team_*` 映射，混用、缺字段和未知字段仍 fail-closed。
- 验证：`uv run ruff check src tests` 通过；完整测试 `2869 passed, 39 subtests passed`；`uv run python -m src.main --validate` 通过，仅保留已接受的 `legacy_allow_all` 环境提示和第三方 protobuf 弃用警告。
- 运行验证：本地 restart 的最新 generation `gnDTI80kXgNZg7TXwSwtVeqE04A8sxgw` 已 ready，PID `2690031` 持续运行，Employee 后台恢复完成，最新启动段无 Router/Gateway 投影错误、outbound audit 错误或 traceback。
- 运行备注：先触发的延迟 `rr` 与随后本地立即 restart 发生重叠，旧 generation 因 PID 被替换出现 `ProcessLookupError`；最新 generation 不受影响。旧进程未在宽限期退出，脚本执行了既有强制终止降级，后续可独立排查关停 drain。

## 普通编程显式模型选择恢复

- 用户验收指出 `/codex` 等显式入口必须保留模型选择交互。根因是全自动精简时删除了普通编程选择卡、级联 renderer、action 与 callback，并让 slash、纯 ENTER intent 和工具按钮直接调用 `enter_mode()`；模型发现、复合值运行时和事务激活底座仍然存在。
- 明确分流契约：六个 `/tool`、工具切换按钮和无参数 `/model` 属于显式配置入口，展示模型选择并等待该次配置选择；SMART 自由文本、`auto_forward`、`pending_prompt` 和已保存配置的续聊仍全自动，不新增任务等待/确认状态。
- 恢复精确能力矩阵：Codex 逐模型读取官方 ACP `model` 与 `reasoning_effort` 响应，不生成笛卡尔积；实测 `gpt-5.5` 仅 low/medium/high/xhigh，`gpt-5.6-sol` 另支持 max/ultra。Traex 仅使用 live model 与本地权威 metadata 的交集。
- 恢复已审 UX：模型/group/profile/effort 级联、保存值默认选中、上游变化重置下游、默认模型、刷新、确认，以及 discovery/activation loading-ready-failed 卡；`ux/acp-model-cascade.html` 补充显式配置与自动任务分流说明。
- 安全边界：callback 只携选择坐标；最终确认忽略客户端原始 model_name，清缓存后重新获取实时能力矩阵，由服务端组合并严格校验，成功启动/切模后才沿既有事务边界持久化。
- 全量测试首次发现既有 restart process-group 清理竞态：SIGKILL 后约 20ms 未观察到退出时原始 `subprocess.TimeoutExpired` 会逃逸。新增确定性 RED 后将其归一为 `RestartGateTimeout`，不扩大 deadline、不吞异常；目标 9 passed。
- 验证：合并定向 52 passed；全仓 Ruff 通过；完整测试 `2910 passed, 39 subtests passed`；配置校验通过，仅有第三方 protobuf 弃用警告和已接受的 `legacy_allow_all` 提示。
- 部署：本地立即 restart 正常优雅停止并启动；generation `gzWTJAiRz5Gbpqbpgsf7YH3mmm5PLcSA`、PID `3119277` ready，Codex 预热 7 模型，Slash Commands 与 Employee 后台恢复完成，最新启动段无错误。

## 模型选择卡飞书 schema 修复

- 生产日志在 04:05:09-11 精确显示三次 `230099`：`ROOT -> body -> elements -> [3](tag: select_static); invalid width`。服务、WebSocket 和模型探测均正常，实际故障是选择卡无法由飞书创建。
- 根因是恢复选择卡时给 `select_static` 写入了浏览器风格的 `"width": "100%"`，该值不属于飞书卡片 schema。
- TDD 新增协议回归先稳定 RED，随后从公共选择器构造点移除非法 width；保留 options、initial_option 与 callback payload。
- 验证：模型选择链合并测试 53 passed，scoped Ruff 通过；本地 restart 成功，generation `g-bgXjJZkvC-UCZPXfIwzdrb3ReKKJRA`、PID `3625899` ready，重启后无新 schema 错误。

## 普通编程模型选择端到端闭环

- 真实租户日志证明下拉与确认回调均到达，04:10:23 ACP 已成功应用 `model`/`reasoning_effort`，但原卡无任何反馈。根因不是 action/ACP，而是 activation 三态 builder 虽存在却从未接入 `_activate_acp_selection`。
- 同轮审计发现并一并修复：callback 继承陈旧 thread ContextVar 会把显式普通入口误建为 topic session；failed 非默认 retry 缺少 group/profile/effort 坐标，实时重组必失败；初次/刷新 loading/error builder 未接线。
- 完整状态序列改为：初次 loading -> selector；级联选择 PATCH 同一卡；confirm 立即 PATCH initializing；仅在 session ready、durable project commit 和 ModeManager 激活后 PATCH ready；entered false/session 缺失/commit 拒绝/异常/submit 异常统一 PATCH failed，PATCH 失败才发送可见兜底卡。
- 显式选择固定为项目级持久普通模式，不重读 callback ambient thread；自动任务/保存配置路径保持全自动且不更新配置卡。callback 入口先清空 ContextVar，只恢复 chat/project 坐标完全匹配的注册线程。
- failed retry 现在携 canonical model/profile/effort，并继续重新获取实时能力矩阵校验；default retry 同样可用。初次与 refresh 先显示 loading，真实异常同卡 error，普通 cascade 使用缓存直接重绘避免抖动。
- 新增真实 `P2CardActionTrigger` 端到端回归，使用官方 `option: str`，覆盖 ws -> registry -> selector PATCH -> confirm -> initializing -> success ready/persist/mode 或 failure rollback/failed，以及陈旧 thread 污染。
- 验证：合并定向 106 passed；全仓 Ruff 通过；完整测试 `2925 passed, 39 subtests passed`；配置校验通过。部署 generation `gyZbQXvCMNYWYTXmVTS-3xm2oDAHDKfY`、PID `3899896` ready，后台恢复完成且启动段无错误。

## 编程卡片 footer 与 Claude transport 探测修复（05:02 UTC）

- 更改：移除共享项目卡默认分支中的 Coco/Claude/Gemini/Traex 跨工具进入引导；已处于旧模式时仍保留退出与切换项目，旧卡片 action handler/config 保留兼容。
- 原因：共享 footer 只识别四种旧 active mode，Codex、Aiden、SMART 和空 mode 会误落入默认分支，在已进入 Codex 后仍显示“进入其他模式”。
- 更改：单工具模型选择改用 provider 注册表作为 fail-closed 白名单，initial/cascade/final 均不再调用 `list_acp_tools()` 做六后端在线探活；伪造 provider 在 fetch/activate 前拒绝。
- 原因：`list_acp_tools()` 会逐一执行 provider availability，Codex 选择因此越界探测 Claude；Claude 实际使用 CLI transport，却被按 ACP Server 能力判断并执行无效 `claude update`。去重仅在单进程有效，重启后会再次发生。
- 更改：新增 transport-aware `is_programming_tool_available()`；Claude 与真实启动契约一致，只检查 `ClaudeCLIConfig.command` 是否在 PATH，其余后端继续使用 ACP registry。`/acp`、`/tools`、`/tools_status` 与 Workflow 发现统一复用该接口。
- 更改：Claude 模型发现不再尝试不存在的 `claude acp serve`；无保存模型时保留后端默认选择，有保存模型时回显该模型。Claude CLI 没有权威本地全量模型枚举接口，因此未编造静态完整目录。
- 更改：Autonomous auxiliary 若配置 Claude，在输入解析/provider resolver/auto-update 前明确 fail-fast；仍保持 `allow_cli=False` 和 deny-all/tool-filter 安全契约。
- 测试：新增 footer、单工具 selector、Claude availability/model discovery、Workflow warmup 与 auxiliary fail-fast 回归；组合回归 `104 passed`。
- 验证：全量 `2946 passed, 39 subtests passed`；`uv run ruff check src tests` 通过；`uv run python -m src.main --validate` 通过。
- 运行：`./restart.sh restart` 成功，新 PID `115983`，generation `gsMgVVAkD9O3ZuU1D8DDJgAhtX9AL94M`；05:01:13 至 05:02:11 的新启动段无 ERROR、Traceback 或 Claude ACP auto-update。
- 已知环境：`INGRESS_ACCESS_MODE=legacy_allow_all` 按用户确认保持；测试仅剩第三方 `lark_channel` protobuf 弃用警告。

## 配置面精简与自动权限契约修复（05:30 UTC）

- 审计：`Settings` 顶层约 210 个字段、Card 17 个字段；`.env.example` 有 17 个不在 schema 且被 `extra=ignore` 静默吞掉的假键，本机 `.env` 45 项中 31 项等于代码默认值、5 项已失效。
- 删除：10 个生产零消费 Settings 字段、仅测试使用的 `Settings.spec_review`/`SpecReviewConfig` 公共视图及 `src/config/spec.py`。
- 示例：删除 17 个假键和 Spec review/retry/circuit、Context 分页/抓取、内部队列/轮询等实现旋钮；保留身份、访问安全、持久化 ABI、协调器、高层 token/队列/执行预算。修正 traex 工具说明、项目群后缀默认值和 Card worker 默认说明。
- 本地部署：在不读取或输出值的前提下，将 `.env` 从 45 个赋值收敛到 9 个；保留 APP 凭据、管理员、入口策略、默认工具、项目后缀、保存的 shell whitelist，以及两个显式 Workflow SLA。该文件被 Git 忽略，不进入提交。
- 安全修复：删除 plain SHA-256 legacy 卡片签名 fallback、进程启动日续期逻辑、兼容窗口枚举及两个配置字段；v1 keyed HMAC 与 v2 chat-bound HMAC 保持。
- 正确性修复：`create_engine_session(auto_approve)` 改为 `bool | None` tri-state，并从 factory 经 SyncACPSession/ACPSession 真正传到 GhostAPClient；显式 True/False 优先，None 才回退全局 Settings。Workflow false 与 Employee true 现有 lane 语义开始真实生效，危险命令、sandbox 和 tool filter 保持 fail-closed。
- 保留边界：未删除/无限化权限、Journal/anchor、上下文 token、附件、队列、并发、卡片包体和终止保险丝；内部参数从公开配置隐藏不等于机制消失。
- 测试：组合回归 `197 passed`；首次全量发现 2 个旧契约并修正；最终全量 `2942 passed, 39 subtests passed`；Ruff 与 `src.main --validate` 通过，仅第三方 protobuf 弃用警告。
- 运行：精简 `.env` 后 `./restart.sh restart` 成功，新 PID `480291`，generation `gIIfdCNOWT35Bh8OVuf1d_SNSqczUF-0`；本次启动至 Employee background recovery 无 ERROR/Traceback。

## Employee roster 查询入口恢复（06:01 UTC）

- 根因：`1ac10623` 退役旧 Slock 产品面时整文件删除了与其共存的现役 Employee 飞书 Handler；产品目录仍声明 `/employees`/`/roster`，SystemHandler 却没有注册，固定落入未知命令。自然语言也没有 roster intent。
- 数据状态：权威 Journal 共 2325 帧、2519 事件，HMAC、sequence、hash chain 与 anchor 一致；6 名 VISIBLE 员工均未丢失，当前 `ACTION_REQUIRED=2`、`ARCHIVED=4`、`ACTIVE=0`。
- 语义缺口：原 `EmployeeMembershipService.list_employees()` 是调度/入群授权 API，只返回 ACTIVE+VISIBLE；即使只恢复命令仍会假空。保留该 fail-closed 合约，新增 `ProductionEmployeeHireService.list_employee_roster()` 返回 tenant 内全部 VISIBLE 生命周期员工并支持归档过滤。
- 入口：新增独立 `EmployeeHandler`；`/employees` 与 `/roster` alias 统一进入 roster；新增精确自然语言 intent（如“有哪些员工”），并用反例防止劫持“实现员工列表页面”等编程任务。
- 卡片：新增数字员工目录卡，展示状态统计、职责、tool/model/profile/effort 与协作群数量；ACTIVE 文案为“在职”而非“在线”。不展示 tenant、owner/bot principal、群 ID、persona、权限、凭据或员工 ID。
- 配置：删除失真的 `EMPLOYEE_DEPARTMENT_ENABLED` 开关；配置姿态改由真实 composition 门 `AUTONOMOUS_VISIBLE_EMPLOYEE_LIMIT > 0` 推导。`--validate` 现正确显示 Employee Department enabled。
- UX：新增 `ux/employee-roster-card.html`，目标形态覆盖 0 active、2 action_required、4 archived。
- 测试：组合回归 `157 passed`；全量 `2969 passed, 39 subtests passed`；Ruff 与配置校验通过，仅第三方 protobuf 弃用警告。
- 真实验证：新 roster API 对当前 Journal 返回 6；卡片 1 页、15 body elements、2083 bytes；禁止字段键/值与员工 ID 泄漏均为 0。
- 运行：`./restart.sh restart` 成功，新 PID `848700`，generation `gybJAKNKZ9aQ5jiwraV-MO4BHrfCeq2M`；Employee background recovery 完成，无 ERROR/Traceback。

## 员工目录隐藏归档并支持职责更新（07:16 UTC）

- 用户反馈：默认数字员工目录不应展示已归档员工；“未设置职责”需要给出真实可执行的设置方式。截图同时暴露了完整 `agt_...` ID 被渲染的问题。
- 目录修复：查询显式使用 `include_archived=False`，卡片构建器再次防御性过滤归档项；标题和统计改为“当前员工”口径；员工 ID 仅作为隐藏排序键，不再进入卡片内容。
- 职责操作：新增 `/employee-role <员工名或ID> <职责>`。命令要求管理员身份和精确 tenant，员工必须可见、未归档且名称唯一；职责规范化后限制为 1 至 80 字符，同值更新不写新事件。
- 持久化：只提交锚定的 `employee.profile_changed {"role": ...}` Journal 事件，并在锚定成功后投影；该操作仅修改职责标签，不改变 persona、capabilities、permissions 或会话。
- 安全与路由：Dispatcher 和 WebSocket 入口均按管理员变更操作保护；sender、管理员和 tenant 缺失时默认拒绝，服务错误对用户脱敏。
- UX：更新 `ux/employee-roster-card.html`，未设置职责时展示针对该员工的 `/employee-role` 示例。
- 验证：目标回归 `122 passed`；全量 `2997 passed, 39 subtests passed`；`ruff check src tests` 和配置验证通过。
- 真实 Journal 核对：当前员工 2 名，均为 `ACTION_REQUIRED`；4 名归档员工未渲染；原始及去 Markdown 转义后的员工 ID 命中均为 0；仅 1 条职责设置提示；卡片 901 bytes。
- 运行态：服务已重启为 PID `1535609`、generation `g1uBRMtXemocVLsQjXtZIw1gxWCN1p0w`；Slash Commands 共 56 条，本次创建 `/employee-role` 1 条；员工恢复完成且启动段无错误。
