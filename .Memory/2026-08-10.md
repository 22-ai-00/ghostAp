# 2026-08-10

## 三目标最终收口与验证

### 结果

- 总代码量从基线 413,233 行降至 205,582 行：生产 Python 136,714 行、Workflow JS 1,855 行、测试 67,013 行。
- 共减少 207,651 行，减幅 50.250%，当前总量为基线的 49.750%。
- 普通编程、Deep、Spec、Workflow 均使用全自动主路径：安全推荐自动采用，未授权高风险自动拒绝，恢复与 continuation 有界，显式 stop/cancel 保留。
- Dynamic Workflow 保持 GhostAP 自有 Node sandbox 与 6+2 原语；卡片完整展示 direct-agent 调度、单条当前操作、attempt、阶段、token、耗时和无截断终态账本。

### 本轮关键修复

- Workflow：修复 `_replace_js_quote` 对双引号字符串中 apostrophe 的误改；`verify()` 强制 closed evidence schema；`race()` 钳制超长 timer、合并历史失败，并用 AsyncLocalStorage 阻止 deadline 后延迟派发；stderr 单行 4 KiB、总计 64 KiB。
- Spec：恢复次数写入 checkpoint，并使用跨进程非阻塞 `flock` lease；持锁重读权威状态并将自动恢复限制为一次。
- Project Chat：明确未提交的绑定 Saga 在补偿后立即中止，不再错误 complete/jump/welcome。
- 普通编程：非流式卡片回退不再解引用空 `prog_session`；六后端仍执行 factory -> prompt；ProgrammingCardSession 使用权威 active-text 集合。
- Autonomous：修复 blocked 通知已删方法、Context composition 剪枝残留、Ingress models 孤立 decorator、测试 helper 导入与 hire port 语法错误。
- Card/Feishu：每测使用独立 DeliveryRegistry 生命周期；清除 reducer 对退役 `_RETRY_ACTIONS` 的引用；失败 Agent token/duration 纳入 Workflow 指标；ChatLockGate 卡片投递失败直接降级文本反馈。
- ACP：迁移官方 schema `KillTerminalCommandResponse` 到 `KillTerminalResponse`；修复 sync adapter 缺失 json 与 event-fence monkeypatch 目标。
- Tasking：终态事件不再重复携带最后的 running progress。

### 测试迁移与精简

- 删除仅保活已退役 UnifiedContextStore CRUD/version/serialization、Workflow preview/menu、legacy ingress/gateway、Spec pause/resume/shared-reviewer 等 facade 的测试。
- 保留并迁移 append-only context、租户/聊天隔离、Journal/Effect 锚定、R4 默认拒绝、自动恢复、锁、补偿、任务/current operation/subagent、终态分页和 HTML fallback 等核心合同。
- 最终 inventory：212 个测试文件、2,399 个测试函数、65,349 测试源代码行、0 个精确重复组。

### 最终验证

- `uv run ruff check src tests`：All checks passed。
- `uv run python scripts/test_inventory.py tests/`：通过，0 duplicate groups。
- `uv run python -m pytest tests/ -q -m "not slow"`：2,832 passed，26 deselected，39 subtests passed。
- `uv run python -m pytest tests/ -q`：2,858 passed，39 subtests passed；唯一 warning 为第三方 lark-channel protobuf 的 datetime 弃用提示。
- `uv run python -m src.main --validate`：配置校验通过。

### 剩余环境提示

- 当前配置 `INGRESS_ACCESS_MODE=legacy_allow_all`，校验器明确标记为 time-bounded break-glass 模式；不是本轮代码失败，但生产部署应按计划切回受限 ingress 策略。
- 第三方 `lark-channel-sdk` 内部仍使用已弃用的 `datetime.utcfromtimestamp()`；本仓库测试不失败，待上游升级。

## 服务启动与主 Bot 回复链修复

- 现象：进程和 WebSocket 可启动，但主 Bot 回复因 outbound audit 被错误替换为不可用占位实现而 fail-closed；同时旧 Journal 在 Router/Gateway 投影恢复时触发 schema 错误，restart readiness 仍被过早发布。
- 根因：`EmployeeDepartmentRuntime` 创建并持有 `_owned_main_bot_send_audit`，但属性与 readiness 读取已删除的 `_main_bot_activation_audit`；精简后移除了历史 `control_consumed`、旧 authorized payload 和 `slock_*` binding 的重放兼容；WebSocket connected 被误当成完整回复链健康。
- 修复：统一 outbound audit 单一事实源；仅在 Journal 重放期间精确兼容已知旧事件/字段，主动写入和公开 schema 继续严格拒绝；readiness 增加主 Bot 回复链与审计可用性门禁，并支持恢复后的晚就绪。
- 安全性：未删除、重置或重写现有 Journal；Gateway 兼容仅接受完整三字段 `slock_* -> team_*` 映射，混用、缺字段和未知字段仍 fail-closed。
- 验证：`uv run ruff check src tests` 通过；完整测试 `2869 passed, 39 subtests passed`；`uv run python -m src.main --validate` 通过，仅保留已接受的 `legacy_allow_all` 环境提示和第三方 protobuf 弃用警告。
- 运行验证：本地 restart 的最新 generation `gnDTI80kXgNZg7TXwSwtVeqE04A8sxgw` 已 ready，PID `2690031` 持续运行，Employee 后台恢复完成，最新启动段无 Router/Gateway 投影错误、outbound audit 错误或 traceback。
- 运行备注：先触发的延迟 `rr` 与随后本地立即 restart 发生重叠，旧 generation 因 PID 被替换出现 `ProcessLookupError`；最新 generation 不受影响。旧进程未在宽限期退出，脚本执行了既有强制终止降级，后续可独立排查关停 drain。

## 普通编程显式模型选择恢复

- 用户验收指出 `/codex` 等显式入口必须保留模型选择交互。根因是全自动精简时删除了普通编程选择卡、级联 renderer、action 与 callback，并让 slash、纯 ENTER intent 和工具按钮直接调用 `enter_mode()`；模型发现、复合值运行时和事务激活底座仍然存在。
- 明确分流契约：六个 `/tool`、工具切换按钮和无参数 `/model` 属于显式配置入口，展示模型选择并等待该次配置选择；SMART 自由文本、`auto_forward`、`pending_prompt` 和已保存配置的续聊仍全自动，不新增任务等待/确认状态。
- 恢复精确能力矩阵：Codex 逐模型读取官方 ACP `model` 与 `reasoning_effort` 响应，不生成笛卡尔积；实测 `gpt-5.5` 仅 low/medium/high/xhigh，`gpt-5.6-sol` 另支持 max/ultra。Traex 仅使用 live model 与本地权威 metadata 的交集。
- 恢复已审 UX：模型/group/profile/effort 级联、保存值默认选中、上游变化重置下游、默认模型、刷新、确认，以及 discovery/activation loading-ready-failed 卡；`ux/acp-model-cascade.html` 补充显式配置与自动任务分流说明。
- 安全边界：callback 只携选择坐标；最终确认忽略客户端原始 model_name，清缓存后重新获取实时能力矩阵，由服务端组合并严格校验，成功启动/切模后才沿既有事务边界持久化。
- 全量测试首次发现既有 restart process-group 清理竞态：SIGKILL 后约 20ms 未观察到退出时原始 `subprocess.TimeoutExpired` 会逃逸。新增确定性 RED 后将其归一为 `RestartGateTimeout`，不扩大 deadline、不吞异常；目标 9 passed。
- 验证：合并定向 52 passed；全仓 Ruff 通过；完整测试 `2910 passed, 39 subtests passed`；配置校验通过，仅有第三方 protobuf 弃用警告和已接受的 `legacy_allow_all` 提示。
- 部署：本地立即 restart 正常优雅停止并启动；generation `gzWTJAiRz5Gbpqbpgsf7YH3mmm5PLcSA`、PID `3119277` ready，Codex 预热 7 模型，Slash Commands 与 Employee 后台恢复完成，最新启动段无错误。

## 模型选择卡飞书 schema 修复

- 生产日志在 04:05:09-11 精确显示三次 `230099`：`ROOT -> body -> elements -> [3](tag: select_static); invalid width`。服务、WebSocket 和模型探测均正常，实际故障是选择卡无法由飞书创建。
- 根因是恢复选择卡时给 `select_static` 写入了浏览器风格的 `"width": "100%"`，该值不属于飞书卡片 schema。
- TDD 新增协议回归先稳定 RED，随后从公共选择器构造点移除非法 width；保留 options、initial_option 与 callback payload。
- 验证：模型选择链合并测试 53 passed，scoped Ruff 通过；本地 restart 成功，generation `g-bgXjJZkvC-UCZPXfIwzdrb3ReKKJRA`、PID `3625899` ready，重启后无新 schema 错误。

## 普通编程模型选择端到端闭环

- 真实租户日志证明下拉与确认回调均到达，04:10:23 ACP 已成功应用 `model`/`reasoning_effort`，但原卡无任何反馈。根因不是 action/ACP，而是 activation 三态 builder 虽存在却从未接入 `_activate_acp_selection`。
- 同轮审计发现并一并修复：callback 继承陈旧 thread ContextVar 会把显式普通入口误建为 topic session；failed 非默认 retry 缺少 group/profile/effort 坐标，实时重组必失败；初次/刷新 loading/error builder 未接线。
- 完整状态序列改为：初次 loading -> selector；级联选择 PATCH 同一卡；confirm 立即 PATCH initializing；仅在 session ready、durable project commit 和 ModeManager 激活后 PATCH ready；entered false/session 缺失/commit 拒绝/异常/submit 异常统一 PATCH failed，PATCH 失败才发送可见兜底卡。
- 显式选择固定为项目级持久普通模式，不重读 callback ambient thread；自动任务/保存配置路径保持全自动且不更新配置卡。callback 入口先清空 ContextVar，只恢复 chat/project 坐标完全匹配的注册线程。
- failed retry 现在携 canonical model/profile/effort，并继续重新获取实时能力矩阵校验；default retry 同样可用。初次与 refresh 先显示 loading，真实异常同卡 error，普通 cascade 使用缓存直接重绘避免抖动。
- 新增真实 `P2CardActionTrigger` 端到端回归，使用官方 `option: str`，覆盖 ws -> registry -> selector PATCH -> confirm -> initializing -> success ready/persist/mode 或 failure rollback/failed，以及陈旧 thread 污染。
- 验证：合并定向 106 passed；全仓 Ruff 通过；完整测试 `2925 passed, 39 subtests passed`；配置校验通过。部署 generation `gyZbQXvCMNYWYTXmVTS-3xm2oDAHDKfY`、PID `3899896` ready，后台恢复完成且启动段无错误。

## 编程卡片 footer 与 Claude transport 探测修复（05:02 UTC）

- 更改：移除共享项目卡默认分支中的 Coco/Claude/Gemini/Traex 跨工具进入引导；已处于旧模式时仍保留退出与切换项目，旧卡片 action handler/config 保留兼容。
- 原因：共享 footer 只识别四种旧 active mode，Codex、Aiden、SMART 和空 mode 会误落入默认分支，在已进入 Codex 后仍显示“进入其他模式”。
- 更改：单工具模型选择改用 provider 注册表作为 fail-closed 白名单，initial/cascade/final 均不再调用 `list_acp_tools()` 做六后端在线探活；伪造 provider 在 fetch/activate 前拒绝。
- 原因：`list_acp_tools()` 会逐一执行 provider availability，Codex 选择因此越界探测 Claude；Claude 实际使用 CLI transport，却被按 ACP Server 能力判断并执行无效 `claude update`。去重仅在单进程有效，重启后会再次发生。
- 更改：新增 transport-aware `is_programming_tool_available()`；Claude 与真实启动契约一致，只检查 `ClaudeCLIConfig.command` 是否在 PATH，其余后端继续使用 ACP registry。`/acp`、`/tools`、`/tools_status` 与 Workflow 发现统一复用该接口。
- 更改：Claude 模型发现不再尝试不存在的 `claude acp serve`；无保存模型时保留后端默认选择，有保存模型时回显该模型。Claude CLI 没有权威本地全量模型枚举接口，因此未编造静态完整目录。
- 更改：Autonomous auxiliary 若配置 Claude，在输入解析/provider resolver/auto-update 前明确 fail-fast；仍保持 `allow_cli=False` 和 deny-all/tool-filter 安全契约。
- 测试：新增 footer、单工具 selector、Claude availability/model discovery、Workflow warmup 与 auxiliary fail-fast 回归；组合回归 `104 passed`。
- 验证：全量 `2946 passed, 39 subtests passed`；`uv run ruff check src tests` 通过；`uv run python -m src.main --validate` 通过。
- 运行：`./restart.sh restart` 成功，新 PID `115983`，generation `gsMgVVAkD9O3ZuU1D8DDJgAhtX9AL94M`；05:01:13 至 05:02:11 的新启动段无 ERROR、Traceback 或 Claude ACP auto-update。
- 已知环境：`INGRESS_ACCESS_MODE=legacy_allow_all` 按用户确认保持；测试仅剩第三方 `lark_channel` protobuf 弃用警告。

## 配置面精简与自动权限契约修复（05:30 UTC）

- 审计：`Settings` 顶层约 210 个字段、Card 17 个字段；`.env.example` 有 17 个不在 schema 且被 `extra=ignore` 静默吞掉的假键，本机 `.env` 45 项中 31 项等于代码默认值、5 项已失效。
- 删除：10 个生产零消费 Settings 字段、仅测试使用的 `Settings.spec_review`/`SpecReviewConfig` 公共视图及 `src/config/spec.py`。
- 示例：删除 17 个假键和 Spec review/retry/circuit、Context 分页/抓取、内部队列/轮询等实现旋钮；保留身份、访问安全、持久化 ABI、协调器、高层 token/队列/执行预算。修正 traex 工具说明、项目群后缀默认值和 Card worker 默认说明。
- 本地部署：在不读取或输出值的前提下，将 `.env` 从 45 个赋值收敛到 9 个；保留 APP 凭据、管理员、入口策略、默认工具、项目后缀、保存的 shell whitelist，以及两个显式 Workflow SLA。该文件被 Git 忽略，不进入提交。
- 安全修复：删除 plain SHA-256 legacy 卡片签名 fallback、进程启动日续期逻辑、兼容窗口枚举及两个配置字段；v1 keyed HMAC 与 v2 chat-bound HMAC 保持。
- 正确性修复：`create_engine_session(auto_approve)` 改为 `bool | None` tri-state，并从 factory 经 SyncACPSession/ACPSession 真正传到 GhostAPClient；显式 True/False 优先，None 才回退全局 Settings。Workflow false 与 Employee true 现有 lane 语义开始真实生效，危险命令、sandbox 和 tool filter 保持 fail-closed。
- 保留边界：未删除/无限化权限、Journal/anchor、上下文 token、附件、队列、并发、卡片包体和终止保险丝；内部参数从公开配置隐藏不等于机制消失。
- 测试：组合回归 `197 passed`；首次全量发现 2 个旧契约并修正；最终全量 `2942 passed, 39 subtests passed`；Ruff 与 `src.main --validate` 通过，仅第三方 protobuf 弃用警告。
- 运行：精简 `.env` 后 `./restart.sh restart` 成功，新 PID `480291`，generation `gIIfdCNOWT35Bh8OVuf1d_SNSqczUF-0`；本次启动至 Employee background recovery 无 ERROR/Traceback。

## Employee roster 查询入口恢复（06:01 UTC）

- 根因：`1ac10623` 退役旧 Slock 产品面时整文件删除了与其共存的现役 Employee 飞书 Handler；产品目录仍声明 `/employees`/`/roster`，SystemHandler 却没有注册，固定落入未知命令。自然语言也没有 roster intent。
- 数据状态：权威 Journal 共 2325 帧、2519 事件，HMAC、sequence、hash chain 与 anchor 一致；6 名 VISIBLE 员工均未丢失，当前 `ACTION_REQUIRED=2`、`ARCHIVED=4`、`ACTIVE=0`。
- 语义缺口：原 `EmployeeMembershipService.list_employees()` 是调度/入群授权 API，只返回 ACTIVE+VISIBLE；即使只恢复命令仍会假空。保留该 fail-closed 合约，新增 `ProductionEmployeeHireService.list_employee_roster()` 返回 tenant 内全部 VISIBLE 生命周期员工并支持归档过滤。
- 入口：新增独立 `EmployeeHandler`；`/employees` 与 `/roster` alias 统一进入 roster；新增精确自然语言 intent（如“有哪些员工”），并用反例防止劫持“实现员工列表页面”等编程任务。
- 卡片：新增数字员工目录卡，展示状态统计、职责、tool/model/profile/effort 与协作群数量；ACTIVE 文案为“在职”而非“在线”。不展示 tenant、owner/bot principal、群 ID、persona、权限、凭据或员工 ID。
- 配置：删除失真的 `EMPLOYEE_DEPARTMENT_ENABLED` 开关；配置姿态改由真实 composition 门 `AUTONOMOUS_VISIBLE_EMPLOYEE_LIMIT > 0` 推导。`--validate` 现正确显示 Employee Department enabled。
- UX：新增 `ux/employee-roster-card.html`，目标形态覆盖 0 active、2 action_required、4 archived。
- 测试：组合回归 `157 passed`；全量 `2969 passed, 39 subtests passed`；Ruff 与配置校验通过，仅第三方 protobuf 弃用警告。
- 真实验证：新 roster API 对当前 Journal 返回 6；卡片 1 页、15 body elements、2083 bytes；禁止字段键/值与员工 ID 泄漏均为 0。
- 运行：`./restart.sh restart` 成功，新 PID `848700`，generation `gybJAKNKZ9aQ5jiwraV-MO4BHrfCeq2M`；Employee background recovery 完成，无 ERROR/Traceback。

## 员工目录隐藏归档并支持职责更新（07:16 UTC）

- 用户反馈：默认数字员工目录不应展示已归档员工；“未设置职责”需要给出真实可执行的设置方式。截图同时暴露了完整 `agt_...` ID 被渲染的问题。
- 目录修复：查询显式使用 `include_archived=False`，卡片构建器再次防御性过滤归档项；标题和统计改为“当前员工”口径；员工 ID 仅作为隐藏排序键，不再进入卡片内容。
- 职责操作：新增 `/employee-role <员工名或ID> <职责>`。命令要求管理员身份和精确 tenant，员工必须可见、未归档且名称唯一；职责规范化后限制为 1 至 80 字符，同值更新不写新事件。
- 持久化：只提交锚定的 `employee.profile_changed {"role": ...}` Journal 事件，并在锚定成功后投影；该操作仅修改职责标签，不改变 persona、capabilities、permissions 或会话。
- 安全与路由：Dispatcher 和 WebSocket 入口均按管理员变更操作保护；sender、管理员和 tenant 缺失时默认拒绝，服务错误对用户脱敏。
- UX：更新 `ux/employee-roster-card.html`，未设置职责时展示针对该员工的 `/employee-role` 示例。
- 验证：目标回归 `122 passed`；全量 `2997 passed, 39 subtests passed`；`ruff check src tests` 和配置验证通过。
- 真实 Journal 核对：当前员工 2 名，均为 `ACTION_REQUIRED`；4 名归档员工未渲染；原始及去 Markdown 转义后的员工 ID 命中均为 0；仅 1 条职责设置提示；卡片 901 bytes。
- 运行态：服务已重启为 PID `1535609`、generation `g1uBRMtXemocVLsQjXtZIw1gxWCN1p0w`；Slash Commands 共 56 条，本次创建 `/employee-role` 1 条；员工恢复完成且启动段无错误。

## 员工目录后端标识转义修复

- 现象：含下划线的后端模型在飞书目录卡片中显示为 `c\_o\_new\_thinking`，而不含下划线的模型正常。
- 根因：员工目录把通用 Markdown 转义后的 token 再放入反引号代码段；飞书在代码段中按字面显示反斜杠。Journal、Roster DTO 和 Handler 均保持原始值，污染仅发生在 `SystemBuilder`。
- 修复：分离 prose 与 code-span 渲染上下文。姓名和职责继续按普通 Markdown 转义；tool/model/profile/effort 先按原始字符规范化和截断，无反引号时直接进入代码段，含反引号时安全降级为转义后的普通文本。
- 边界：补充 `&<>` 和截断顺序回归，避免 HTML 实体扩张造成字面显示或截断半个实体。
- TDD：首轮 RED `2 failed, 19 passed`，边界 RED `1 failed, 21 passed`；最终目标测试 `22 passed`。
- 扩大验证：相关 7 个卡片/系统测试文件 `132 passed, 1 warning`，scoped Ruff 通过；唯一 warning 为既有第三方 `lark_channel` protobuf 弃用提示。
- 真实 Journal 渲染：2 名当前员工中 1 个后端含下划线，解码 Markdown 与序列化 JSON 的反斜杠下划线命中均为 0；代码段平衡；无归档身份或员工 ID 泄漏；卡片 895 bytes。

## 普通编程任务完成对账假阴性修复

- 事故：`ryingwriter` 任务已交付 10 张 PNG 和 ZIP，两名审查子 Agent 的 rollout 与最终 `list_agents` 均为 `completed`，但 GhostAP 最终仍报“2 个工具调用未进入终态”。
- 时间线证据：两名 child 在 08:22:36/08:22:47 写入最终 `task_complete`；父 Agent 在 08:25:19 调用无过滤 `list_agents` 得到两者 completed，08:25:31 再次完成；08:25:32 classifier 仍读取 ACP 陈旧 `running`，形成 `other:completed[running]` 两项。
- 根因：官方 Codex ACP 的 stale `PromptResult` 有稳定 child thread ID，但缺少 agent path；现有 child rollout monitor 因无法建立 source/path 关联而不产证，随后安全短路又阻止使用父 rollout 中的权威状态。
- 修复：仅从当前父 rollout 的 bounded tail、当前逻辑任务窗口内读取 `sub_agent_activity`，严格校验 source/path 双向唯一、direct path、时间与 activity 类型，再补给既有 monitor。最终状态仍必须由 child rollout 最新 generation 的终态证明；`saw_rollout_candidate` fail-closed 短路保留。
- List fallback 加固：只接受 arguments 精确为 `{}` 的无过滤 `list_agents`，按 JSONL 追加顺序选择窗口内最后调用，并要求唯一 `/root`。
- 竞态与身份加固：JSONL 尾部无换行或任意坏 JSON 时本轮不使用旧证据并在 settle 窗口重试；direct/nested 的 source、agent path、parent thread path 链及 session meta 必须一致；带真实 turn ID 的 active generation 不允许被缺少 turn ID 的晚到 terminal 关闭。
- 卡片投影实验曾尝试让嵌入 child 状态覆盖 outer DONE，但独立审查发现 malformed/mixed/generation 风险，已完整撤回；本次通过纠正最终权威状态消除真实事故中的矛盾，不放宽 outcome。
- TDD：首轮真实事故与 list 严格性 RED `4 failed / 8 passed`；竞态/身份 RED `6 failed / 2 passed`；最终 reconciliation `35 passed`。
- 验证：扩大回归 `295 passed`；全量 `3018 passed, 39 subtests passed`；scoped Ruff 通过。唯一 warning 为既有第三方 `lark_channel` protobuf 弃用提示。
- 真实事故只读回放：修复前 `incomplete` 且未终态 2，修复后 `completed` 且未终态 0；1 条权威 evidence 覆盖两名 completed child；耗时约 39.2 ms。

## ACTION_REQUIRED 员工启动自愈恢复

- 根因：Hire reducer 已支持 `hire.channel.phase_only_recovery` 与 `employee.state_changed(validating)` 的精确原子帧，但生产端 producer 被精简删除；Runtime 又在后台 recovery coroutine 真正完成前返回，Slash `recovery_exhausted` effect 会被错误复用。
- 安全边界：只接受 VISIBLE、当前 workforce/hire 均为 ACTION_REQUIRED、完整 prior automatic activation、当前 Slash/Channel 证据精确匹配、所有 effects 终态、跨 writer epoch 的严格 crash→validating→Slash PREPARED→EXECUTING→ACTION_REQUIRED(recovery_exhausted)→phase ACTION_REQUIRED 尾序列。注册、凭据、Channel `start-*`、未决或额外相关事实全部跳过。
- 原子恢复：持 workforce→hire→Journal 锁重新同步并复核后，以 intent/agent aggregate versions 和 Journal head CAS 提交既有两事件 frame；只有 anchor 成功才同步 workforce/hire 投影并把 intent 交给 Runtime，anchor/CAS 失败不启动外部调用。
- 继续执行：旧失败 effect 原样保留；已锚定 repair context 才允许同 generation 选择新的 Slash attempt，Channel 使用自然的 N+1 generation。fresh Channel READY 后签发 generation-bound challenge，随后才进入 READY_PENDING_VERIFICATION 和 automatic ACTIVE。
- 生命周期：`EmployeeDepartmentRuntime.recover()` 改为进程内 single-flight，并等待真实 coroutine 完成；返回 `eligible/recovered/skipped/failed` 汇总。重复调用共享同一 Future/result，不重复 marker、Slash 或 worker。WS 仅在该结果返回后记录精确计数。
- TDD：首轮 RED `7 failed`，覆盖缺失 producer 与 recover 提前完成；真实 ACTIVE 路径追加 RED 捕获 verification 恢复缺口。首轮独立审查以 `3 failed, 7 passed` 捕获伪 challenge、batch 部分失败仍派发和永久异常 Future；二次审查以 `5 failed, 10 passed` 捕获生产重试不可达、batch half-commit 和混合终态汇总丢失。最终 focused recovery/hire/startup/WS 回归 `54 passed`，scoped Ruff 通过；唯一 warning 为既有第三方 `lark_channel` protobuf 弃用提示。
- 独立审查修正：automatic activation 的既有 reducer 合同要求激活前 `verification_consumed=False` 并由 activation 自身置 True，因此不能伪造 fresh challenge 只为翻转该位。恢复分支现仅复用 marker 后 N+1 generation 的已提交 Slash/Channel 证据；batch 任一 Journal/anchor 失败整批不向 Runtime 发布 intent，Runtime 当轮在任何外呼前返回；single-flight 首次异常清理共享 Future 并只允许一次重试，第二次失败保持共享以阻止无界循环。
- 二次审查修正：WS 后台生产入口在同一 recovery worker 内严格执行至多两次共享 single-flight 调用，第二次仍失败才关闭 admission；所有 eligible intents 的 marker/state pairs 现在组成一个 Journal transaction/frame，共享一次 head/aggregate CAS 和 anchor，不存在持久 half-commit；正常与 isolation-error 出口都逐 intent 读取 durable phase，已 ACTIVE 员工计入 recovered，其余精确计入 failed。
- 风险：现生产 Channel 失败使用 `start-*`、`invalid-ready` 等 outcome，不等价于 replay-safe `recovery_exhausted`，本轮继续 fail-closed，未引入 Channel attempt suffix 或放宽 identity/READY 证据。

## 员工所有者 P2P 私聊直达执行

- 新增显式 `EmployeeAuthorizationScope`，仅允许 `MANAGED_GROUP` 与 `OWNER_P2P`；scope 从 Router authority 贯穿 Context request/message scope、Projected binding 和 Gateway dispatch binding，禁止通过伪造 `member_groups` 表达私聊授权。
- OWNER_P2P 仅在官方 SDK 归一化的 `chat_type=p2p`、user sender、同 tenant、持久化 hire requester union 精确匹配时，将 employee-App open_id 映射为 canonical owner principal；删除跨 App open_id 相等捷径，缺失/错误 union 静默 fail-closed。
- Router 与 Gateway 保留 ACTIVE/VISIBLE、bot/app/credential、READY generation/connection、requester ACL、projection head 和锁内 authority 重检；P2P 仅跳过 managed-group registry、member_groups 与 membership-health。
- Context 对 OWNER_P2P 只读取当前 P2P/thread 与员工 L1，完全跳过 chat-wide group recent、L2 group memory 和 group-ledger partial fallback；当前消息仍绑定 source employee-App open_id，ACL/取消绑定 canonical owner。
- 生产 drain 对 OWNER_P2P 跳过主 Bot 群命令门和群 ledger；`/stop` 通过同一 union-owner bridge 使用 canonical owner 调用 cancellation，现有 Outbox target/reply 与幂等恢复链保持不变。
- Router/Gateway 正常构造必须显式提供 scope；仅 replay/projection 对精确旧 schema 缺 scope 的记录回填 `MANAGED_GROUP`，混合、缺失其他字段或额外字段继续拒绝。
- TDD RED：新增测试首次因缺少 `src.autonomous.authorization` 在 collection 阶段产生 4 errors。
- GREEN：受影响 contract/integration/security/chaos 合并集 `238 passed, 1 warning`；`uv run ruff check src/autonomous/ ...` 全部通过。唯一 warning 为 lark-channel protobuf 的上游 `utcfromtimestamp` 弃用提示。
- 未运行全量 `tests/autonomous/`；本次验证覆盖 SDK ingress、Router、Context、Registry、Gateway 实际 Actor 执行/replay、production drain、stop control 与 ingress recovery。

## OWNER_P2P authorization, recovery retry, and context priority follow-up

- Tightened `OWNER_P2P` authorization so Router defaults fail closed without an explicit union-id resolver, `/stop` revalidates the current projected employee owner against the durable hire requester, and gateway dispatch revalidates the exact frozen Channel connection plus SDK identity app under the dispatch lock.
- Kept managed-group behavior intact while preventing direct-message leakage: P2P completion omits group/thread `MEMORY_SUMMARY`; P2P context reads the complete paginated topic and employee-private L1 only, with zero group recent, group L2, or ledger fallback. Managed group mentions retain bounded recent/L2 and their scoped summary.
- Changed context priority to `topic/thread -> employee-private L1 -> recent group -> group L2`. Budget trimming is now `L2 -> recent group -> L1`; topic messages are never partially removed, and a topic that alone exceeds the hard limit fails closed with `BUDGET`. Bumped the prompt render contract to v3 for the new canonical field order.
- Fixed group-ledger partial identity binding to compare the current message sender with the source open-id rather than the canonical owner principal.
- Fixed replay-safe ACTION_REQUIRED repair eligibility to count Slash exhaustion only in the current target generation, so a disposed prior generation no longer blocks a later independent recovery while duplicate attempts in the same generation remain bounded.
- Made a failed durable repair result raise from runtime recovery without poisoning execution blockers. The existing single-flight Future and WS startup loop can now perform the intended one bounded retry; a second failure stays cached and fail-closed.
- Added regression coverage for multi-generation recovery, real Runtime-to-WS retry, owner/connection/identity drift, P2P resolver denial, direct/group artifact separation, complete multi-page topic retention, managed-group budget priority, P2P hostile group isolation, prompt ordering, and L1 Journal/Blob reconstruction after restart with tenant/agent/owner binding.
- Validation: focused RED cases failed at the intended boundaries before implementation; focused GREEN was `15 passed`; expanded affected integration/security/chaos suite was `172 passed, 1 warning`; post-style-fix regression was `1 passed, 1 warning`; Ruff passed for every changed Python file. The warning is the existing `lark_channel` protobuf `utcfromtimestamp()` deprecation.

## Group-ledger source identity and partial budget follow-up

- Kept union-id resolution as the managed-group authorization gate, but persisted the SDK/employee-app source open-id from ingress metadata in `GroupEventPayload`; canonical main-app owner identity remains ACL-only. Added a write-to-partial roundtrip where source and canonical open-ids differ.
- Added shared `context/budget.py` policy used by both full thread assembly and canonical ledger partial assembly. Both now trim `L2 -> oldest group recent -> L1`, never trim topic content, and fail with `BUDGET` when the protected topic alone exceeds the hard limit.
- Canonical partial assembly now separates records belonging to the current topic/root from ordinary group history before budgeting, preserves all retained topic messages, reports accurate layers/trimming trace, and keeps the topic watermark count/digest topic-scoped.
- TDD evidence: the two new regressions initially failed because the ledger stored the canonical owner and partial returned `BUDGET` before trimming. Focused GREEN was `5 passed`; expanded context/runtime/security/chaos coverage was `43 passed`; Ruff passed for all touched files.

## Group-ledger topic-safe windowing follow-up

- Moved `max_group_messages` enforcement after ledger topic/group classification. The current root, current message, all records bound to the current Feishu topic, and supported legacy root-bound topic records are preserved without count truncation; only ordinary non-topic group history is limited to the newest configured records.
- The resulting records are restored to canonical Journal order before partial assembly and shared budget enforcement, so an oversized complete topic now fails `BUDGET` instead of being silently shortened.
- TDD evidence: the new regression initially retained only the current message from a four-message topic when `max_group_messages=2`; focused GREEN was `4 passed`; the full context-service integration module was `22 passed`; Ruff passed for the touched source and test.

## Recovery leaf-lock annotation follow-up

- Updated the `_recovery_lock` inline annotation to the repository's exact leaf-lock marker required by `TestLeafLockAnnotationScan`; lock type, acquisition behavior, and single-flight recovery semantics are unchanged.
- Validation: reproduced the full-suite's sole RED as `1 failed` at `composition.py:552`; the complete targeted lock-ordering module then passed `18 passed`; scoped Ruff passed.

## Production recovery writer-epoch boundary fix

- Live read-only Journal forensics found both visible ACTION_REQUIRED employees satisfied every exact recovery predicate except the historical requirement that Channel crash and subsequent Slash exhaustion occur in different writer epochs. Production performs both in one runtime epoch and restarts only after the terminal failure, unlike the original fixture which restarted between crash and failure.
- Added the authoritative read-only `JournalWriter.writer_epoch` API. Recovery now retains the exact six-frame event/payload/order checks and the internal epoch grouping checks, but requires the terminal ACTION_REQUIRED tail epoch to differ from the writer epoch currently executing recovery. Same-epoch direct recovery remains fail-closed; a restarted writer can reopen the exact terminal history.
- Added a production-shaped regression that records crash, validating, Slash prepared/executing/recovery_exhausted, and employee ACTION_REQUIRED in one epoch; direct recovery is skipped, then the next writer epoch recovers it.
- Validation: after the API-enabling RED, the production-shaped test failed at the expected restarted eligibility assertion; focused recovery coverage was `8 passed`; a read-only probe against both live terminal tails reported both eligible under the corrected boundary; full recovery integration plus hire-saga chaos was `20 passed, 1 warning`; scoped Ruff passed. The warning is the existing `lark_channel` protobuf deprecation.

## Workflow Agent Pool 全自动编排与会话竞态收口

### 根因与产品契约

- 8 月 8/9 日的全自动精简删除了 Workflow Agent 选择入口，生成路径因此退回 registry 默认前三名，用户无法约束实际参与编排的 tool/model。Traex 同时沿用固定 180 秒等待并连续发生三次 timeout，缺少 activity-aware idle 与跨 fallback 的共享 hard deadline。
- Workflow 改为 selection-first：`/wf` 首屏先展示 owner-bound Agent Pool，池规模严格为 1–8；完全相同的 tool/model 组合拒绝，同一 tool 的不同 model 允许并存；成员使用稳定 `A1`、`A2` 等标识。
- 编排器可由用户指定池内成员，也可使用 Auto 从已确认池中选择；用户仅确认一次“使用此池开始编排”。确认后 Agent Pool 与编排器冻结，脚本生成、验证和执行全自动继续，不再要求评审 Agent 选择、脚本确认或继续批准。
- Prompt、fallback、`WorkflowRunSpec` 与运行时均严格限定在冻结池内。直接和动态 Agent 调用必须使用 `agentId`，脚本不能用 tool/model 覆盖绑定；`meta.agentPlan` 用稳定 A-id 表达静态节点，并用 `runtime: true` 与 `candidateAgentIds` 表达运行时候选及实际运行计划。

### 超时、终态与结构性竞态修复

- 脚本生成使用 120 秒 activity-idle timeout，并由所有池内 fallback 共享 600 秒 hard deadline；每个唯一池成员最多尝试一次，切换成员不重置 hard deadline。耗尽生成、验证或执行预算进入 `FAILED`，用户停止进入 `CANCELLED`。
- 生成中或执行中再次发送 `/wf` 会被明确拒绝；用户可等待终态或使用 `/stop_wf`。stop、scheduler admission/submit 失败、queued worker 未准入和 owner retire/release 均补齐确定性清理，防止陈旧生成会话、脚本和 lifecycle owner 重叠。
- ModelFailure wrapper 的 compaction 与 failover 统一为 strict replacement 事务：旧 inner close 未确认时不创建 candidate；candidate 只有在 tool filter 安装成功且未取消后才原子 swap；拒绝 candidate 的 close 若不确定则保留在 `uncertain_sessions`，由 wrapper `close()` 后续重试全部引用。tool-filter 继承、cancel gate 与最大活跃 session 为 1 均有回归合同。
- Workflow generation owner 的 create/bind/send 与新 `/wf` admission 改为锁内权威复核；`GENERATING_SCRIPT` 或仍有 active generation session 时禁止 supersede。`_abandon_start` 在 done/retire 后显式 release，重复失败与 scheduler 失败不再累积 retired owner。
- UX 预览与 AGENTS Workflow 说明同步删除旧三步选择、独立评审和脚本确认契约，只保留 Agent Pool 单次确认后的全自动主路径。

### TDD 与验证

- TDD RED 覆盖默认前三名越界、pool/agentId 绑定、Traex timeout、fallback deadline、明确终态、stop/scheduler 清理、tool-filter replacement、candidate 引用回收、generation supersede TOCTOU 与 lifecycle owner 泄漏；修复后逐层转为 GREEN。
- Wrapper 结构修复的核心 RED 为 `7 failed`，随后定向 `7 passed`、focused `81 passed`；leaf-lock 扫描另由 `1 failed, 17 passed` 转为 `18 passed`。
- 最终完整测试为 `3140 passed, 39 subtests passed`；Ruff 与 `uv run python -m src.main --validate` 通过。仅保留既有 warnings，无新增测试失败或静态检查错误。

### 后续风险

- 需重启后真实飞书首屏/回调验收；本轮没有把该运行态验收虚构为已完成。
