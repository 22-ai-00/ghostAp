# 2026-08-17

## 仓库根目录清理

- 删除无引用的 Git 跟踪杂项：空的 `.codex`、一次性诊断脚本
  `test_logger.py`，以及已被当前 Agent Pool 契约取代的
  `SPEC-WORKFLOW.md`。
- 按用户确认，将工作区中既有的四个 `.superpowers/sdd/task-*-report.md`
  删除纳入同一提交；这些历史报告仍可从 Git 历史恢复。
- 删除可重建缓存与索引：所有仓库内 `__pycache__`/`*.pyc`、
  `.mypy_cache`、`.pytest_cache`、`.ruff_cache`、`.uv-cache`、
  `.codegraph` 和 `.planning`。
- 删除空壳目录 `.agents`、`.trae`、`data`、`workflows`、
  `src/loop_engine`、`src/session`、`tests/test_project_chat`，以及旧版
  `.ghostap-restart` 仓库内锁目录。
- 用户确认不再需要历史恢复后，删除 `.spec_engine` 旧 Spec 产物、
  `picturechat` 历史聊天图片、`.ghostap` 已终止 Workflow 历史与 Journal、
  `stress_test.log` 和 `logs.log`。
- `.venv` 正被 GhostAP PID 2073629 使用，因此先通过
  `./restart.sh stop` 优雅停止服务，再删除环境；停止流程同时清除了
  `.ghostap.pid`。保留 `.env`、`.aiden` 和 `.claude`。
- 清理前盘点估算共释放约 479 MiB。服务当前保持停止；再次运行前需要
  执行 `uv sync --group dev` 重建环境。

### 验证

- `uv run python -m pytest tests/test_docs_references.py -q`：
  `11 passed`。
- `uv run python scripts/test_inventory.py tests/`：成功，288 个测试文件、
  3486 个测试函数、0 组完全重复。
- `git diff --check`：通过。
- 删除后逐项检查授权目标、仓库内 `__pycache__`、`*.pyc` 和 `*.pyo`：
  无残留。
- `./restart.sh status`：确认 GhostAP 未运行。

### 后续风险

- `.spec_engine`、`picturechat`、`.ghostap` 和未跟踪日志不在 Git 中，
  本次删除不可通过仓库恢复。
- 外部已有的 `tail -f logs.log` 进程仍在等待该路径，但不再占用仓库中的
  日志文件；未擅自终止该用户进程。
