# 2026-09-14

## Spec 通用任务与任务驱动专家审查

- 用户要求：Spec 不限于编程；写作包含思路、读者、编辑、审稿视角，其他任务先拆出领域专家，再进行多角色深查；保持其余自动执行和验收规则。
- 根因：Spec/Plan/Task/Build 提示默认软件工作；Review 以关键词分类后套固定角色组，容易把非编程任务按代码变更识别为编程。
- 修改：SpecArtifact 新增可持久化的 required_experts，每个角色包含名称、必要性、关注点和必查项。ReviewArtifacts 传递同一专家计划，Review 直接生成对应角色及稳定 role_id，不再用领域关键词选择模板。独立 completion_control、总角色上限、阻断建议规则和自动收敛逻辑继续保留。
- 角色推断在 SPEC 阶段完成。新生成、直接加载 JSON 和 discovery 后续工作项统一检查 3–7 个完整且名称归一化后互异的专家；不合格时保留原任务、工作目录和规格，自动修复一次，耗尽明确失败。旧数据仍可反序列化，真正执行前补齐专家。
- discovery 正常生成提示包含专家结构；解析失败的最小兜底不伪造通用角色，交给执行阶段进行真实推断。SPEC 只在校验通过后发送一次完成回调，合法直接加载也成对发送开始/完成事件。
- Spec/Plan/Task/Build 提示、BUILD 阶段名称和报告文案泛化为交付；写作提示明确要求创意/选题、目标读者、编辑、审稿/事实或论证核查四个独立视角。
- 回归覆盖：写作和陌生领域角色、结构化产物兼容、原任务修复上下文、重复/不完整/超量角色、直接加载与 discovery fallback、阶段完成回调。
- 验证：角色相关子集 68 passed；主代理补充执行 `uv run python -m pytest tests/test_spec_adaptive_roles.py -q`，退出码 0，7 passed。独立 verifier 运行角色/卡片/引擎/审查子集，退出码 0，70 passed；静态审查 PASS。相关 Ruff 与 `git diff --check` 均通过。
- 限制：尚未通过真实 ACP 后端执行写作任务或进行飞书租户实机验收；专家质量仍由后端根据任务推断，结构校验负责完整性与独立性，不替代事实判断。

## Spec 主卡聚合与容量续接

- 用户截图中的无用子卡源于 SpecStreamProcessor 的 TaskOrchestrator：即使普通任务卡配置默认关闭，动态子代理仍可以创建独立消息；按迭代轮次 rotate 也持续增加卡片。
- 先创建 `ux/spec-adaptive-review.html` 展示主卡中的任务、专家分工、审查结论和折叠过程。生产 SpecCardSession 复用已有 ProgrammingCardSession 的子代理状态投影、来源隔离、工具配对、媒体归属和终态栅栏，不再创建任务级卡或按轮次换卡。
- Spec 主卡根据实际渲染分页结果判断容量，不采用普通编程的 12 个进展块语义分段。容量续卡沿用先创建可见续卡、再封存旧卡的机制，额外重播 Spec 轮次、当前阶段和验收状态，避免续页收到 PHASE_DONE 时找不到阶段块。
- cycle reducer 对显式 retain_full_history 状态保留跨轮历史，其他卡片仍保持原有清理行为。Spec 渲染启用主/子来源分块及折叠工具记录；通知、规划字段和帮助文案改为任务中性表述。
- SPEC 阶段完成后在主卡显示本轮专家及职责，隐藏主代理原始结构化 JSON；子代理文本保留。终态使用已有 finish/fail/cancel 处理并停止 heartbeat，拒绝晚到 ACP 输出。续卡继承引擎 TTL 和任务开始时间。
- 回归：47 项计划与两个子代理只创建一张主卡，跨轮保留文本与专家摘要；展示推断角色、不展示原始 JSON；成功/失败/取消后不接受晚到输出；容量续卡保留全部证据和当前 phase/criteria。卡片相关子集 43 passed，补充渲染子集 39 passed，均退出码 0。
- 最终全量验证：`uv run python -m pytest tests/ -q`，退出码 0，4625 passed、43 subtests passed，用时 181.24 秒。日志 `/tmp/ghostap-spec-final-tests.log`。唯一 warning 来自第三方 lark_channel 的 utcfromtimestamp 弃用，不是本次改动失败。
- 相关 Ruff、`uv run python -m src.main --validate`、`git diff --check` 均退出码 0；独立 verifier 完成源码审阅和 70 项相关测试，判定 PASS。
- 交付：角色优化独立提交 `e22fa7de` 已推送 dev；卡片优化另作聚焦提交。未重启运行中的服务，未向飞书发送测试消息，实机投递外观尚未验收。
