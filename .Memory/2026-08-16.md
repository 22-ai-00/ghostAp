# 2026-08-16

## 修复 macOS restart readiness 误超时

### 任务与现象

- 用户执行 `./restart.sh` 后，Python 服务日志已出现
  `GhostAP restart readiness published generation=...`，脚本仍在 60 秒后返回
  `启动失败：服务未在 60s 内通过 readiness 检查`。
- 故障期间 Codex ACP fallback 安装成功，真实模型预热得到 7 个模型；依赖同步不是阻塞点。

### 根因

- `get_running_pids()` 遍历系统全部进程时，先对每个 PID 调用 macOS `lsof`
  检查 cwd，最后才过滤命令。进程较多时，一次发现扫描即可耗尽 readiness 预算。
- macOS `launchctl remove` 是异步停止：旧 GhostAP 仍会继续优雅退出约 5 秒。
  脚本若立即复用相同 label，`launchctl submit` 可能没有产生新的可运行进程。
- `launchctl submit` 后原实现把 `STARTED_PID` 留空，readiness 只能再次依赖全系统
  进程扫描，无法使用 launchd 已知的权威 PID。

### 修改

- `pid_is_ghostap_service()` 先按精确启动命令过滤，再对候选 PID 检查项目 cwd，
  避免对无关系统进程执行 `lsof`。
- 新增 launchd PID 查询、旧 job PID 退出等待和新 job PID 有界获取；启动后将
  launchd 返回的 PID 直接传给 readiness gate。
- `stop_service()` 在移除 launchd job 前固定已发现的服务 PID，并等待这些 PID
  退出；`launchctl submit` 或 PID 获取失败现在立即返回明确启动失败，不再空等 60 秒。
- 为命令预过滤、旧 launchd PID 等待、新 PID 获取和启动失败传播补充回归测试。

### 验证

- `uv run python -m pytest tests/test_restart_script.py -q`：`24 passed`。
- `uv run python -m src.main --validate`：配置校验通过，无安全发现。
- `bash -n restart.sh` 与 `git diff --check`：通过。
- 真实 `./restart.sh restart`：11.52 秒返回成功；PID `59530`，日志记录
  `[RESTART] start ready ... generation=ghxIK5YuyiQdkZ5Acwb9I_JfWUPpIqVg`。
- `./restart.sh status`、PID/命令检查及 `restart_gate ready` 均确认同一运行实例；
  Codex 模型预热 `count=7`，主 Agent 58 条 Slash Commands 同步完成。

### 后续风险

- 本机 Aiden/Gemini/Traex 命令缺失、Grok 未认证仍会产生非阻塞预热警告；它们不影响
  GhostAP 主服务、飞书长连接或 Codex ACP readiness。

## 本机 Host Shell 完全放开

### 任务与根因

- 用户已明确授权完全放开自己的 Host Shell，并要求能够切换到
  `/Users/xuaner/aiwork/xhs`。
- 失败来自 `PROJECT_ALLOWED_ROOTS` 未配置时采用 `~/workspaces`、`~/work` 默认值；
  `BaseHandler.set_working_dir()` 因而拒绝 `~/aiwork` 下的目标目录。
- 这是本机 `.env` 配置问题，不需要修改生产代码。命令白名单原本已关闭，目录根限制是
  此次错误的直接原因。

### 本机配置

- 将 `SHELL_ACCESS_MODE` 设为 `trusted_local`，并用
  `SHELL_TRUSTED_LOCAL_ACK=true` 显式确认主机级执行风险。
- 将 `PROJECT_ALLOWED_ROOTS=[]` 设为空列表；现有代码契约中空列表表示允许任意目录。
- 保持 `SANDBOX_USE_WHITELIST=false`，并清空命令白名单与黑名单。
- `.env` 继续由 `.gitignore` 排除，未提交任何本机配置或身份数据。

### 验证

- `uv run python -m src.main --validate`：`SHELL_ACCESS_MODE=trusted_local`，无安全阻断项。
- Settings 实读：风险确认 true、允许根 `[]`、白名单关闭、命令白/黑名单均为空。
- `set_working_dir()` 对 `/Users/xuaner/aiwork/xhs` 返回成功。
- 在目标目录用真实 `SandboxExecutor` 执行旧白名单之外的复合命令
  `uname -s && printf shell-open`：返回码 0，输出 `Darwin` 与 `shell-open`。
- 重启耗时 10.63 秒；PID `66493`，readiness generation
  `gGhRKjb1uaS3_SoKwyhKXlepsc1MpWVE`，Codex 模型预热 `count=7`。

## ACP 懒启动与 Bot readiness 解耦

### 现象与根因

- `restart.sh` 在每次启动前默认执行
  `npx --yes @agentclientprotocol/codex-acp@1.2.0 --version`；npm 网络慢或不可用时，
  Bot 进程尚未启动就会被 Codex adapter 预热拖住。
- `Application.run()` 又默认并发探测 Coco 以及六个 ACP 工具的模型目录；虽然为
  daemon thread 且不 `join`，仍会在飞书长连接 readiness 前创建 Codex/TraeX 等
  ACP 进程、发起网络请求并与主启动争用资源。

### 修改

- `GHOSTAP_PREPARE_CODEX_ACP` 的 restart 默认值从 `1` 改为 `0`；默认路径仅记录
  `preheat skipped`，不调用 native Codex 探测或 `npx`。仍保留显式设为 `1` 的运维
  opt-in，不删除固定 adapter 版本和预装能力。
- `acp_model_preheat_on_startup` 默认值改为 `False`，`Application.run()` 的缺省回退也
  改为 `False`。Bot 启动/readiness 不再主动建立任何 ACP 连接；真实 `/codex`、
  模型选择或编程任务仍通过原有 session factory 按需建连。
- ACP 交互期启动仍使用有界重试；首次网络失败不写入半成品 session，用户下一次
  飞书激活可重新创建。新增回归冻结“首次失败、第二次成功”契约。

### 验证与后续风险

- `tests/test_restart_script.py`：`26 passed`，包含默认路径不调用 Codex/native/npx 外部
  探测、主进程不预热以及显式 opt-in 仍可用。
- ACP 启动/模型缓存/直接编程/restart 相关套件：`186 passed`；非 slow 全量：
  `4326 passed, 26 deselected, 43 subtests passed`。
- `bash -n restart.sh`、受影响文件 Ruff、`git diff --check`、配置校验与测试资产
  清单均通过；当前实读 `acp_model_preheat_on_startup=False`，且无环境变量反向覆盖。
- 取舍是首次打开模型选择或 Codex 会话需承担冷启动；这个延迟现在只影响该次
  编程交互，不再影响飞书 Bot 上线。若网络仍不可用，交互返回明确失败后可再试。

## A2A 出站协议内核落地

### 范围与架构决策

- 复核 2026-08-14 的 A2A 研究，并用官方固定版本证据补齐实现契约：规范发布版
  `v1.0.1`、wire `protocolVersion=1.0`、Python SDK `a2a-sdk==1.1.2` 分别冻结，
  不把三者混为一个版本。
- A2A 只承担 Employee Department 向远端持久 Agent 的出站协作边界；Employee
  身份与 Team 编排保持稳定，Coco/Codex 等本地编程工具继续走 ACP，不用 A2A
  替换 ACP session。
- 后续生产接线点固定为 `TeamBackend` 的 local/remote 组合路由，而不是
  `EmployeeActor`、`EmployeeSessionHost` 或 ACP gateway。本轮先交付可独立调用、可恢复的
  Phase 1 内核，不启用生产 Team 远端路由。
- 暂不接线是正确性门禁：现有 Team coordinator 在 effect 已锚定 executing、远端
  submit 已成功、assignment.submitted 尚未落 Journal 之间存在崩溃窗口。生产启用前必须
  先让 executing 恢复只走 GetTask/Subscribe 对账，并在远端 claimed completion 后完成
  本地 Artifact 重组、锚定与验证。

### 实现

- 新增 SDK-free 的冻结远端领域契约、派发 port 与 Journal 可重放投影；稳定绑定
  `run/assignment/attempt/acceptance/message/context/task`、远端身份、tenant、endpoint、
  binding、wire 版本与 Card digest。远端 `COMPLETED` 只映射为
  `CLAIMED_COMPLETED`，不能直接成为本地已验证终态。
- 新增严格 Agent Card 边界：64 KiB 上限、重复键/非有限数/未知 protobuf 字段拒绝、
  canonical SHA-256 固定、配置 endpoint 与 JSON-RPC 1.0 interface 必须精确一致，Card
  不能反向改写出站目标。首期拒绝 required extension、URL/raw part 和 push。
- 新增响应 codec：1 MiB 上限、八种任务状态完整映射、message/context/task 身份核验、
  text/data part 白名单、Artifact append sequence 与 `last_chunk` 终结约束。
- 新增 `RemoteDispatchLedger`：敏感 instruction/observation 先加密发布到 BlobStore，
  再在 `transaction_guard` 中以 CAS 和 anchored Journal event 持久化；支持 prepare、
  executing、task binding、observation、send uncertain、cancel 的幂等重放和重启恢复。
- 新增官方 SDK JSON-RPC 1.0 客户端适配器，覆盖 SendStreamingMessage、GetTask、
  SubscribeToTask 与 CancelTask。公共 Card 必须无凭据获取并验证成功后才解析 RFC 6750
  bearer；客户端关闭环境代理和重定向，限制连接、超时、unary/SSE 大小并严格解析
  JSON-RPC envelope。
- SSRF 防线同时校验每次 DNS 解析的全部地址和 TCP 实际 peer，拒绝 private、loopback、
  link-local、reserved、IPv4-mapped IPv6、混合解析与 DNS rebinding；peer 在 TLS/HTTP
  或 Authorization 字节发送前核验。
- 恢复规则固定为：已有 task id 只 Subscribe/GetTask，不重新发送；executing 但没有
  task id 时先锚定 `SEND_UNCERTAIN` 并失败关闭。每个响应按 bind task、codec、
  Blob/Journal、yield 的顺序处理，确保调用者看见的状态已经耐久。
- 依赖显式固定 `a2a-sdk==1.1.2` 与 direct `httpx==0.28.1`。重锁同时升级了
  `requests` 并引入 Google auth/protobuf 依赖，因此单独扩大 Lark/Feishu 回归。

### 验证

- A2A 领域、Card/codec、Journal、真实 SDK wire、客户端与重启恢复：`96 passed`。
- 完整 Autonomous：sandbox 内除 16 个必须监听回环 socket 的既有 Channel 合约外
  `1453 passed`；这 16 项在允许本地 socket 的隔离运行中由 JUnit 确认 `16 passed`。
- 受依赖重锁影响的其余 Lark/Feishu 套件：`81 passed`。
- 仓库级非 slow：`4437 passed, 26 deselected, 43 subtests passed`。
- `uv run ruff check src/autonomous/`、`uv lock --check`、`git diff --check`、
  `scripts/test_inventory.py tests/` 均通过；`src.main --validate` 通过，保留现有
  `ingress_legacy_allow_all` break-glass 警告，本轮未改变该配置。
- 独立安全边界复核确认：连接时 peer 校验发生在凭据发送前；创建期取消会完成
  HTTP 资源清理；close 失败或取消后仍可重试；没有剩余阻止提交的问题。

### 提交与后续门禁

- `acc98ba7`：远端派发领域契约与 ADR。
- `32910c14`：官方 SDK 一手证据补充。
- `92d9c32b`：Card/codec 信任边界与精确依赖。
- `e91ee598`：可恢复 Journal 账本。
- `ef535008`：安全出站客户端、真实 wire 合约和跨重启集成测试。
- 下一阶段只有在修复 Team 外呼崩溃窗口、加入 remote descriptor 配置/凭据 resolver、
  composite backend 生命周期以及 claimed completion 本地验证后，才允许生产远端路由。
