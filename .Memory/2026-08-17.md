# 2026-08-17

## 仓库根目录清理

- 删除无引用的 Git 跟踪杂项：空的 `.codex`、一次性诊断脚本
  `test_logger.py`，以及已被当前 Agent Pool 契约取代的
  `SPEC-WORKFLOW.md`。
- 按用户确认，将工作区中既有的四个 `.superpowers/sdd/task-*-report.md`
  删除纳入同一提交；这些历史报告仍可从 Git 历史恢复。
- 删除可重建缓存与索引：所有仓库内 `__pycache__`/`*.pyc`、
  `.mypy_cache`、`.pytest_cache`、`.ruff_cache`、`.uv-cache`、
  `.codegraph` 和 `.planning`。
- 删除空壳目录 `.agents`、`.trae`、`data`、`workflows`、
  `src/loop_engine`、`src/session`、`tests/test_project_chat`，以及旧版
  `.ghostap-restart` 仓库内锁目录。
- 用户确认不再需要历史恢复后，删除 `.spec_engine` 旧 Spec 产物、
  `picturechat` 历史聊天图片、`.ghostap` 已终止 Workflow 历史与 Journal、
  `stress_test.log` 和 `logs.log`。
- `.venv` 正被 GhostAP PID 2073629 使用，因此先通过
  `./restart.sh stop` 优雅停止服务，再删除环境；停止流程同时清除了
  `.ghostap.pid`。保留 `.env`、`.aiden` 和 `.claude`。
- 清理前盘点估算共释放约 479 MiB。服务当前保持停止；再次运行前需要
  执行 `uv sync --group dev` 重建环境。

### 验证

- `uv run python -m pytest tests/test_docs_references.py -q`：
  `11 passed`。
- `uv run python scripts/test_inventory.py tests/`：成功，288 个测试文件、
  3486 个测试函数、0 组完全重复。
- `git diff --check`：通过。
- 删除后逐项检查授权目标、仓库内 `__pycache__`、`*.pyc` 和 `*.pyo`：
  无残留。
- `./restart.sh status`：确认 GhostAP 未运行。

### 后续风险

- `.spec_engine`、`picturechat`、`.ghostap` 和未跟踪日志不在 Git 中，
  本次删除不可通过仓库恢复。
- 外部已有的 `tail -f logs.log` 进程仍在等待该路径，但不再占用仓库中的
  日志文件；未擅自终止该用户进程。

## 修改后自动推送规则

- 将 `AGENTS.md` 中“只有用户明确要求时才 push”改为强制交付规则：每次
  修改验证并创建聚焦 commit 后，必须立即 push 到当前所在分支。
- push 失败时必须查明原因并明确报告；未成功 push 不得视为任务完成。
- 原因：确保本地完成状态与对应远端分支持续一致，避免已提交修改滞留在
  本地工作区。
- 验证：检查 `AGENTS.md` 仅保留新的 push 规则，并运行
  `git diff --check`。
- 后续风险：该规则会让后续任务默认产生远端变更；仍禁止 force push，
  普通 push 冲突需要先安全解决后再完成任务。

## Employee reporting Journal 热循环修复

- 现场采样确认 `employee-durable-reporting` 是此前约 65% 单核常驻 CPU 的
  来源：每个 reporting tick 都经 `EmployeeFireService.list_states()` 调用
  `JournalWriter.replay()`，反复读盘、验 HMAC 并 JSON 解码 5 MiB、3996 帧
  的 Employee Journal。
- 将 Fire 的状态读取、提交前检查、外部变更 fence 恢复统一改为
  `JournalWriter.committed_tail(1)`。该 tail 由进程内唯一 writer 在加载和
  每次锚定提交时维护，仍以 Journal/anchor 为唯一事实源，不新增可写缓存，
  也不放宽断链或 anchor 不匹配检查。
- 同步调整跨服务游标推进回归，显式证明活跃 drain 使用 committed tail，
  空闲 drain 不访问 Journal，且 Fire 路径不再退回全盘 replay。

### 验证

- `uv run ruff check src/autonomous/provisioning/fire_service.py
  tests/autonomous/integration/test_employee_fire_authority.py`：通过。
- `uv run pytest tests/autonomous/integration/test_employee_fire_authority.py -q`：
  `30 passed`。
- reporting、离职交付、恢复与启动顺序扩展回归：`114 passed`。
- 真实重启后 `py-spy` 显示 `employee-durable-reporting` 稳定阻塞在 Event
  wait；后台恢复完成后的 2 秒采样 CPU 从约 65% 降为 0.5%。

### 后续风险

- 主 Bot 发送审计 Journal 已达 382 MiB / 382771 帧，仍在主 WebSocket
  前同步全量恢复，导致约 40 秒启动延迟和约 1.7 GiB RSS；这是下一阶段
  单独修复项，不与本次 Fire 投影变更混合。
