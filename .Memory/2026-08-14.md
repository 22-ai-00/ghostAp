# 2026-08-14

## dev 发布与安全远程重启调度

- 将本地 `dev` 相对 `origin/dev` 的 13 个连续提交快进推送到 GitHub；远端从
  `ad5840ec49da65bb890d436e5d948368f82d6e3a` 更新为
  `0e1e73c3ec7736fa4bfcdae4710ec8d68818bcb8`。发布范围包含 ACP 稳定性、普通编程
  路由、卡片续接去重/每卡 12 个进展块及对应回归测试。
- 发布前已完成相关测试 232 passed、非 slow 全量 4317 passed（另有 26 deselected、
  43 subtests passed）、配置校验、Ruff、测试资产清单与 diff 检查；未跟踪的
  `node_modules/`、`package.json`、`package-lock.json` 未纳入提交或推送。
- 重启前服务 PID 为 `3057863`，ready generation 为
  `g7tkM8Q9Tlwn4472aA_33pssrxEKqjOg`。`./restart.sh rr` 于 02:17:25 UTC 成功调度
  独立 worker，worker PID `1784734` 于 02:17:26 UTC 进入安全重启脚本并绑定该
  generation。
- 当前 Bot 任务仍持有 drain reader，因此记录时旧 PID/generation 保持不变；worker
  存活并等待任务终态后再停服、启动和发布新 readiness。此处只记录“已安全调度”，
  不把触发回执误写为重启完成；最终结果应以本任务结束后的
  `[RESTART] start ready` 与 `[RESTART] remote worker done` 为准。
