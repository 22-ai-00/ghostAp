# 2026-07-31

## 测试套件语义去重与快慢分层

### 现场与根因

- 初始清单有 701 个测试文件、11,589 个 AST 测试函数、273,220 行和 12,983 个 pytest collection item。AST 只找到 6 组完全相同函数体，均是刻意保留的 Deep/Spec 独立引擎或 Coco/TTADK 后端契约，不能按文本相似直接删除。
- 主要冗余来自语义层：命令和路由表通过参数化为每个等价输入重复创建重型 fixture，Workflow 对完整 action 注册表另做严格子集断言，部分测试复制生产公式或在测试内部手工模拟 handler，UI/错误格式也存在同一生产边界的重复样例。
- 默认快速层还收集了真实 sleep、子进程超时/回收、沙箱证明和租约续期契约。它们并非重复测试，但会把本应快速反馈的门禁变成墙钟等待。
- 审计时发现 `TimerScheduler.shutdown()` 只在 `join(timeout=2)` 前唤醒一次，却要到 `join()` 后才清理队列；调度线程醒来后会再次等待远期任务，资源清理因而稳定等待两秒，并可能留下仍存活线程。这是生产实现竞态，不应靠删测试隐藏。

### 变更

- 将命令目录、命令注册、Workflow action 和 WebSocket 路由的等价参数改为单测试内表驱动循环；保留所有原始输入及逐输入失败诊断，同时让重型 client/registry fixture 只构造一次。
- 删除严格被完整注册表、规范格式器、真实 handler、redaction/error 规范套件覆盖的子集断言，以及复制生产公式和手工模拟分支的伪契约。
- 保留安全、授权、并发、Journal/effect、恢复、持久化、外部协议以及 Deep/Spec、Coco/TTADK 的独立契约；6 组精确重复函数体数量保持不变。
- 把 17 个经实测超过一秒、且必须依赖真实进程或墙钟的用例标记为既有 `slow` 层。测试未删除，完整发布门禁仍会全部运行。
- `TimerScheduler` 增加 shutdown fence：先发布关闭状态，再在锁内取消 scheduler event、清空队列，随后唤醒工作线程并等待退出；调度与关闭竞态会在同一锁下拒绝新任务。新增远期定时器快速退出、回调不执行和 schedule/shutdown 竞态回归。

### 结果与验证

- 最终清单为 701 个文件、11,541 个 AST 测试函数、272,869 行和 12,753 个 collection item；净减 230 个收集项、48 个函数和 351 行，未增加精确重复组。
- 默认快速层：`uv run python -m pytest tests/ -q -m "not slow"` 为 12,626 passed、9 skipped、118 deselected、45 subtests passed，耗时 460.81 秒。
- 慢速层：`uv run python -m pytest tests/ -q -m slow` 为 118 passed、12,635 deselected，耗时 630.11 秒；新标记的 17 项单独验证为 17 passed。
- 命令/路由和删除项的 canonical owners 为 219 passed；Timer/resource cleanup 为 24 passed；卡片边界为 325 passed；Workflow 相关为 184 passed。
- `uv run python scripts/test_inventory.py tests/`、全量 collection、`uv run python -m src.main --validate`、全部改动 Python 文件 Ruff 和 `git diff --check` 均通过。只保留两条固定版本 `lark_channel` 第三方 deprecation warning。

### 后续风险

- 快速层仍约 7 分 40 秒；后续若继续优化，应优先对剩余真实时间契约引入 fake clock 或更轻的进程夹具，不能以删除安全/恢复边界换速度。
- 慢速层约 10 分 30 秒，仍是发布门禁的一部分；本次只改变调度层级，没有降低完整测试覆盖。

## 任务耗时与 Workflow 选择复审补强

### 变更

- TTL、强制关闭和审批拒绝路径统一在终态事件中携带会话时钟，使页脚在超时、拒绝及强制收口时冻结真实耗时。
- Worktree 的 `completed_empty` 纳入终态冻结；若随后收到合法进度恢复事件，则清除旧冻结值并继续按原会话起点计时。
- 活跃 CardKit `element_content` 流式更新时，页面签名忽略仅由秒数变化造成的页脚差异，避免每秒变化迫使整卡 PATCH；工具、模型和状态仍参与全局签名，下一次正常全卡更新仍渲染最新耗时。
- Workflow 评审选择恢复严格互斥：显式打开/选择评审会关闭 Auto，启用 Auto 会清空显式评审和面板；旧快照同时含两种状态时按显式评审规范化。重复选择 Traex 后模型面板仍保持可继续添加。

### 验证

- 页脚、终态、投递、Session、Worktree 和 Workflow renderer 针对性回归 325 passed。
- Workflow 选择与相邻处理器回归 184 passed；覆盖“第一个 Traex → 第二个不同模型 → 完成”、Auto/显式评审互斥、picker callback 和旧状态恢复。
- 回归证明仅耗时秒数变化不改变活跃流式页签名，而工具或模型变化仍会触发完整签名变化；未新增独立 heartbeat 或飞书 API 调用。

### 后续风险

- 运行中耗时仍随既有业务更新刷新，不承诺无事件时主动秒级 PATCH。
- 尚未在真实飞书租户完成卡片桌面/移动端和完整 Traex 重复选择链路验收；本地覆盖状态机、Card 2.0 JSON、分页签名与交付决策。

## Agent 平台、耐久编排与主动工作演进计划

### 分析结论

- 结合原始 Agent Department 需求、`docs/goals.md`、Autonomous/Employee 设计与 ADR、7 月项目记忆、现有产品收敛计划及生产代码完成只读审计。当前优势不是单点功能数量，而是已经同时具备多 Backend 会话、Direct/Deep/Spec、Workflow DSL、Employee Actor、Team Coordinator 和 Journal/Effect/Vault 安全基座；主要缺口是这些能力尚未形成统一且诚实的接入、控制、编排与主动任务闭环。
- 明确采用“一个控制面、三条执行通道”：普通 Direct Agent 编程保持零额外 LLM hop；Deep/Spec 作为成熟保护路径仅做窄适配和局部正确性修复；Worktree/Workflow/Team/Slock 作为开发中试验路径，优先承接类型化 IR、Artifact、预算、取消、恢复和 Trigger。
- 发现并纳入 Phase 0 的事实缺口包括 Backend 工厂/能力声明漂移、Claude CLI model 选择纵向合同、Workflow model/reviewer 绑定、Worktree 超时与终态、Spec completion fail-open，以及 Team/Slock 辅助 Agent 工具权限。它们是 2026-07-31 快照的计划输入，实施前必须在目标分支复现，避免覆盖并行 Workflow 工作。
- 主动工作被定义为“Trigger + GoalTemplate + AuthorityEnvelope 产生同一 Run”，而不是新增一个 Engine；默认从 assist/read-only 开始，必须具备 owner、scope、TTL、budget、去重、misfire、approval、kill switch 和 unknown Effect 处置。
- 当前单机文件内核的能力上限被明确限制在受信工程主机上的可恢复 Agent Department；多副本、跨区域、强防回滚和无人监管跨系统自治需要外部一致性、KMS/身份、强隔离及独立 Hardened Profile。

### 产出

- 新增 `docs/2026-07-31-agent-platform-evolution-plan.md`，包含当前能力矩阵、根因、目标架构、方案权衡、边界与天花板、42 个可执行任务、工程检查点、测试命令、投入估算、成功指标、风险、Stop-Expansion Rules 和审计追踪。
- 计划把 `docs/2026-07-30-ghostap-product-convergence-plan.md` 的 Task 1–24 与 Task 26 本机部分作为 Foundation Track，显式复用其产品、安全、上下文/路由/任务控制、运行生命周期、诊断和备份；原 Task 25 租户 Beta Gate 不再作为前置。
- 三个并行 subagent 分别完成 Agent 接入、编排和主动安全审计；据此保留 family/binding 有效能力、唯一 durable aggregate/scheduler/kill owner、opaque Effect 边界、固定 namespace/ACL、数据外发授权和删除恢复高水位，同时删除不适合当前单用户场景的灰度与发布矩阵。
- 本轮仅新增计划与记忆，不修改生产代码、配置或卡片行为，也未触碰工作区中并行存在的 Workflow Handler/Engine/测试和卡片洪泛修复计划。

### 单用户直接体验修订

- 部署画像明确为“单 Owner、单主机、当前账号”：功能通过对应合同、安全和恢复测试后直接进入正常菜单/命令，不等待租户 allowlist、百分比放量、Beta/RC 晋级、长观察窗口或签名发布包。
- 完成度收敛为 `mature/developing/unavailable` 的说明标签，不再控制曝光；Workflow IR v2 采用“零派发编译检查 → 耐久执行检查 → Owner 直接可选”，v1 只作紧急回退。
- 主动服务完成后默认可用但无 Trigger 时空闲；每个 Trigger 由 Owner 显式创建/启用，权限模式只有 `assist_read_only` 与逐 Run 确认的 `supervised_write`，不再使用 `shadow_observe` 或逐租户状态。
- Phase E 从发布体系改为 8–15 天的 Owner 端到端清单、默认 8 Employee 工作负载基线、备份恢复和直接启用/紧急回退；10/50 Employee 与 48 小时 soak 仅按真实扩容或排障需求执行。
- 单用户简化没有删除安全边界：Owner principal、project/chat scope、固定 `tenant_key` namespace、data egress、PermissionProfile、Effect 锚定、unknown reconcile、预算、kill、删除高水位和恢复仍是硬合同。

### 验证

- `uv run pytest tests/test_docs_references.py -q`：4 passed，仅保留两条既有 `lark_channel` 第三方 deprecation warning。
- 修订后计划仍为 42 个任务、220 个 Markdown 围栏且全部成对；`git diff --no-index --check /dev/null docs/2026-07-31-agent-platform-evolution-plan.md` 无空白错误。
- 定向扫描确认不存在 `DG-*`、Decision Gate、线上 shadow、逐租户开放、release bundle 或 eligibility matrix 等遗留执行路径；`allowlist` 仅保留在否定发布逻辑或网络外发安全说明中。

## Workflow 卡片重复推送与生命周期竞态修复

### 现场与根因

- `logs.log` 显示 05:18:58 评审结束、05:19:01 开始生成；随后到 05:21:55 约每 8 秒一次 generation heartbeat PATCH 均失败，累计 23 次 `main Bot patch recipient scope is unavailable`。05:22:01 生成超时后运行时启动，05:22:06–05:23:55 又约每 10 秒失败一次，且关停开始后仍继续出现。
- 首张 Workflow 选择卡没有把入站 `/wf` 的可信 `origin_message_id` 写入 `MessageLinker`，因此后续 PATCH 无法通过 main Bot recipient-scope 审计。旧进度回调在每次 PATCH 失败后又执行 `send_card_to_chat()`，把每个 heartbeat 都退化为一条新的飞书卡片；新卡同样没有 origin，形成永久失败循环。
- 深入并发审计还发现旧 selection/generation/queued-start 可在新请求后恢复、共享脚本路径可被旧生成覆盖、runtime bridge worker 可越过终态复用下一轮 engine 字段、selection finish 与按钮点击会丢更新、慢渲染可用旧卡覆盖新卡、cleanup retry 会丢失未完成 owner tombstone。这些问题会放大卡片重复、旧任务复活和状态串线。

### 变更

- 保持 main Bot outbound audit 默认拒绝，不放宽权限；首张选择卡、生成卡、初始进度卡和有界终态 fallback 全程传递可信 origin。周期性 generation/runtime progress 只允许 PATCH，失败时不再 CREATE；终态替代卡最多创建一次。
- 引入 session-keyed lifecycle owner，统一 selection、生成、卡片投递、排队启动和 runtime 终态。stop/cancel/supersede 会设置同一 stop fence、停止 heartbeat、等待在途投递并只清理 owner 自己的脚本。
- 同一发起者或管理员的新 `/wf` 采用 newest-wins：旧的未完成 selection、生成和未 claim 的 queued start 可直接撤销，只保留最新任务；已经 claim 的真实 runtime 不被隐式替换，仍需显式停止，避免共享 bridge/executor 未静默前复入。
- 生成脚本改为 session 唯一源文件，确认执行使用只读语义的唯一临时副本；取消/失败只删除本 owner 的产物，成功源文件保留到下一次 Workflow admission，继续支持 `/wf_save`。
- RuntimeBridge 在 stop 后关闭新提交门、终止 Node、等待 agent/subworkflow future 和 AgentExecutor 真正静默后才发布 IDLE。静默失败时 engine 保持 STOPPING tombstone；started/preclaim owner 通过持久 retired-owner 集合跨多次 cleanup 保留，直到真实 `done_event`。
- selection mutation、finish snapshot 和状态迁移统一采用 engine → selection 锁序；legacy confirm 同一 CAS 内读取工具并迁移状态。卡片投递比较本次 render snapshot 与当前 snapshot，并持联合锁更新，慢 S1 不会覆盖较新 S2。
- 新增确定性 barrier 回归，覆盖 PATCH 失败不 CREATE、origin 链接、stale callback/newest-wins、cancel/stop/confirm 竞态、唯一 artifact、bridge worker drain、queued preclaim cleanup、重复 manager cleanup、finish/select、legacy confirm/select 及乱序卡片渲染。

### 验证

- `uv run pytest tests/test_workflow*.py -q`：929 passed、8 skipped、45 subtests passed。
- `uv run python -m pytest tests/ -q -m "not slow"`：12,664 passed、9 skipped、118 deselected、45 subtests passed，耗时 460.82 秒。
- 生命周期/确认/选择定向复核 150 passed；selection/card 独立终审 145 passed；queued cleanup 双重回归 2 passed。两轮并发终审最终均为 Ready，无 Critical/Important 遗留。
- `uv run python -m src.main --validate`、`uv run python scripts/test_inventory.py tests/`、全部改动 Python 文件 Ruff 和 `git diff --check` 通过。Inventory 为 703 个测试文件、11,579 个 AST 测试函数、6 组既有精确重复。

### 后续风险

- 尚未在真实飞书租户复演故障注入；本地回归已证明持续 PATCH 失败时周期进度不会创建新卡，且所有有界 fallback 都继承可信 origin。
- 仍保留固定版本 `lark-channel-sdk` 的两条 Python 3.13 上游弃用警告，沿用 B054 跟踪，不通过过滤器掩盖。
- 终审发现 `SelectionFlowController.snapshot()` 未持久化 `error_message`，空选择时可能不显示预期内联错误；不影响状态阻断、卡片洪泛或 newest-wins 合同，记录为 B057 后续处理。

## Agent 平台实施基线、产品合同与安全姿态

### 审计与依赖

- 在 `dev@8d15f26ba6874ab9e19f6972bad7b30dd953a540` 上完成 Foundation
  Task 1–24、Phase 0–E 的逐项只读审计；没有任务满足新计划的完整验收，
  但识别出可复用的 Employee Journal/Vault/Context/Team、Session factory、
  Workflow 和 TaskScheduler 底座。
- 新增 `docs/2026-07-31-agent-platform-dependency-ledger.md`，为所有任务记录
  唯一实现归属、重叠合并、串行依赖、当前 `missing/partial/complete` 状态和硬验收。
  Foundation Task 25 被明确删除，Task 26 仅保留本机启用/回退/验证。
- 受限 sandbox 内的 asyncio 跨线程 self-pipe 唤醒失效，导致 Employee 异步测试
  假超时；最小 asyncio 探针在 sandbox 内复现、隔离环境外正常。未把环境问题当成
  代码回归，改在隔离环境外运行干净非慢速基线：
  `12664 passed, 9 skipped, 118 deselected, 45 subtests passed`。

### 公共产品合同

- 新增 `docs/product-contract.md`，把 GhostAP 定义为受信工程环境中的飞书原生
  Agent Department Gateway：主 Bot 是控制面，Employee Bot 是独立执行身份，
  provider/model/engine 可替换，Direct 保持零额外规划调用。
- README、`pyproject.toml` 和 `docs/goals.md` 对齐生产事实；Host Shell 明确为
  特权宿主执行而非操作系统沙箱，单机文件持久化不承诺多副本线性一致性或特权
  主机防回滚。删除 `/goal` 和任意 `/hire --prompt` 的可用性承诺。
- 文档合同测试锁定精确 package 描述、核心身份/执行/持久化边界，并扫描所有
  fenced command example，阻止 `/hire ... --prompt` 变体重新进入公共文档。

### 类型化安全姿态

- 新增纯函数 `evaluate_security_posture()` 及冻结的 ingress/shell mode、
  finding/severity/posture 类型，稳定报告 Shell admin/allowlist/isolation/
  trusted-local、非强制 ingress、管理员引导和 Employee retention finding code。
- 新设置使用安全默认值：ingress `enforced`、Shell `disabled`、Employee Department
  显式关闭、群上下文保留 30 天。完整实施请求被记录为推荐的首次管理员
  `p2p_only` 合同授权，并同步更新 `AGENTS.md`，避免只在运行代码中静默收紧。
- `--validate` 新增 `[安全姿态]`，blocking finding 返回非零；singleton 启动日志
  用稳定 code 显示 warning/blocker。`.env.example` 和 README 同步配置语义。

### 验证

- 产品合同：`tests/test_docs_references.py` 6 passed，独立复审 APPROVED。
- 安全姿态、配置和 validate：121 passed；实际
  `uv run python -m src.main --validate` 返回 0 并显示无 security finding；
  独立复审在修正 DG-1A 持久规则和稳定 code 测试后 APPROVED。
- 相关 Ruff 和 `git diff --check` 通过；只保留固定版本 Lark 依赖的两条上游
  deprecation warning。

## Task 0.7 辅助 Agent deny-all 工具权限

### 变更

- 新增共享 `AuxiliaryToolProfile.DENY_ALL` 与 fail-closed 安装函数；未知工具同样拒绝，
  后端缺少可执行的 `set_tool_filter` 时关闭会话并失败，不静默降级。
- 新增 `create_auxiliary_session()`，用于纯文本协调、意图分类和摘要会话。它保留安全的
  rate-limit wrapper，但不使用可能替换内部会话并丢失过滤器的 model-failure wrapper；
  无法强制过滤的 TTADK CLI 在启动前拒绝。
- Team coordinator、Slock NLI classifier 和 discussion summarizer 已切换到辅助会话；
  Employee 工作执行仍使用原有执行会话与 capability/ACL 路径，没有收窄。
- 独立复审发现 ACP permission bridge 只在 `execute` 且存在 command 时调用过滤器；
  现已在 auto-approve 前将所有 permission kind 和原始输入稳定映射到工具请求。
  `read/edit/delete/move/search/fetch/think/switch_mode/other`、无 command execute 与
  未知 kind 均进入 `_is_tool_allowed()`；过滤器异常继续 fail-closed，有 command
  execute 的 Git/Shell 分类、危险命令和 sandbox 检查保持不变。

### 验证

- TDD 红灯：新增 6 个安全测试初始全部失败，证明协调器、分类器和摘要器未安装过滤器，
  且共享辅助工厂尚不存在。
- 首轮复审补测使用真实 `GhostAPClient.request_permission()` 驱动注入，参数化全部
  permission kind，红灯稳定复现 `28 failed`；另以 1 个红测锁定畸形 execute command
  延续既有 fail-closed。
- 计划指定的两个安全测试文件 `17 passed`；与完整 `tests/test_acp_client.py`
  合并复跑为 `97 passed`。独立复审另取权限桥、无过滤器兼容、ACP 0.11、Employee
  isolation 和真实 Git bridge 得到 `41 passed`，结论 APPROVED。
- Task 0.7 Team/NLI/摘要目标与相邻套件 `210 passed`；完整
  `tests/test_acp_client.py` 加 Employee isolation、真实 Git permission bridge 和
  backend contract 回归曾单独复跑为 `98 passed`；改动文件 Ruff 全通过。
- 受限 sandbox 的 asyncio 跨线程 self-pipe 唤醒缺陷会让 NLI 慢启动用例在 worker
  已关闭后挂起；未改生产逻辑规避环境问题，隔离环境外复跑通过。

### 后续风险

- 当前辅助 profile 仅有严格 deny-all，尚未实现计划后续 A1–A5 的细粒度工具能力。
- 支持辅助会话的新 CLI 后端必须先提供可验证的工具过滤强制执行，不能仅保存 callback。
- 提交为 `f69acc90`。

## Task 0.6 Spec completion 默认拒绝

### 变更

- completion-control 明确 `FAIL`、`GOAL_NOT_MET`、非 JSON、非对象 JSON、缺 verdict、
  缺证据、缺失/重复 role 或 `role_id` 不匹配均不能转换成通过。
- 格式失败与有效评审失败分开处理；前者在同一 role runner、同一 provider/model
  binding 和同一 prompt 上使用既有上限有界重试，耗尽后持久化 `PAUSED`，明确等待
  人工确认后恢复。
- Engine 从原始 `role_outcomes` 重新验证 completion gate，不信任陈旧或不一致投影；
  Spec 原阶段、provider/model 选择、startup-timeout fallback 和 UI 未迁移到新 IR。

### 验证

- 红测依次复现显式失败/格式失败/缺 verdict、非对象 JSON、陈旧投影绕过、role
  缺失/重复、identity 不匹配和“只写重试文案但未真重试”。
- 隔离环境外计划指定套件 229 passed，相邻 adaptive strategy/timeout/
  provider-selection/reliability/completion-control 114 passed；Ruff 和
  `git diff --check` 通过。
- 两轮独立复审在补齐真实有界重试、PAUSED 人工确认和 role identity 后 APPROVED；
  提交为 `93a52598`。

## Task 0.4 Workflow 执行绑定与 Reviewer 承诺

### 变更

- 新增独立的 `src/workflow_engine/run_spec.py`：`WorkflowRunSpec` 为深冻结合同，
  一次性记录 orchestrator、显式 reviewers、完整 tool-model map、task、chat/topic、
  agent-call budget、单调时钟 deadline、initiator、工具白名单和显式 Auto 语义；
  model map 与确认绑定不一致、Auto 与显式 Reviewer 并存、显式模式无 Reviewer 时均
  默认拒绝。
- Workflow confirm 在最终 engine/session CAS 内从权威 pending 快照构造唯一 RunSpec，
  后台闭包只把同一个冻结对象传给 Engine，不再平行传递 requirement、selected_tools、
  initiator 并让 Engine 重建缺绑定 project。RunSpec 的审计副本进入 `WorkflowProject`
  和最终原始状态报告；旧的直接 Engine API 只在边界转换成标记清楚的兼容 RunSpec。
- Engine 对每次 JS `agent()` 调用强制应用确认后的 tool/model；即使生成脚本自带其他
  model，或用户确认的是默认模型，也以 RunSpec 为准。RunSpec budget 在每次调用前
  执行，deadline 同时传给 RuntimeBridge，并与每次 AgentExecutor deadline 取更早者。
- 显式 Reviewer 在主 Workflow 产物返回后逐个执行新的真实 AgentExecutor 调用，禁用
  Journal cache；每次调用的 selection key、tool/model、状态、输出/错误、token、耗时和
  fresh/cached 事实持久化为 `ReviewerEvidence`。`AgentExecutor` 还会保留 ACP 的真实
  `stop_reason`；Reviewer 只有 `end_turn` 且输出非空才能成功，max_tokens/cancelled、
  缺 stop reason、空输出或 backend error 均失败关闭。任何承诺 Reviewer 失败都会让
  整个 Workflow 失败，不能发布“已评审”完成态。
- `AgentProgress`、进度卡执行流和完整 Markdown/HTML 报告补充 model/role；确认摘要、
  执行 trace、最终报告分别展示同一 orchestrator/reviewer tool-model 绑定，报告只根据
  实际 ReviewerEvidence 声称独立评审，不从 UI 选择反推“已评审”。
- Review Auto 布尔值从选择完成经过脚本生成 pending 保持到确认卡和 RunSpec，不再用
  空 reviewer list 推断新运行；仅为旧持久状态保留显式标注的兼容恢复分支。Auto 的
  选择卡、确认卡与生成提示均改为“无独立 Reviewer；编排器负责最终检查”，不再声称
  存在无法验证的主 Agent 自评审。

### TDD 与验证

- 首轮红灯：新增两份计划测试收集时稳定得到 2 个 `WorkflowRunSpec` ImportError；
  最小模型落地后继续得到 3 个 `execute_workflow(run_spec=...)` TypeError。后续红灯分别
  锁定 handler 仍传平行 kwargs、trace 缺 model/role、确认卡不接受显式 Auto，以及
  RunSpec 未拒绝矛盾 tool-model map。
- `tests/test_workflow_execution_bindings.py` 与
  `tests/test_workflow_reviewer_contract.py` 最终 `16 passed`，覆盖深冻结、handler→engine
  同对象、每次调用 model pin、budget/deadline、Auto、独立 Reviewer、失败关闭、证据
  round-trip、真实 stop reason 传播、四类不完整终态、确认摘要、trace 和最终报告一致性。
- 独立复审追加的首轮红灯稳定得到 `6 failed`：Auto 卡片仍声称自评审、Executor 丢失
  stop reason，以及 max_tokens/cancelled/缺 stop reason/空输出四种 Reviewer 伪完成；
  修复后专项 `11 passed`，Executor cancellation/fault-tolerance 相邻回归也纳入全量验证。
- 首轮全 Workflow 回归的 3 个失败全部查明：旧单测用 `WorkflowEngine.__new__` 绕过
  初始化，缺少 `_run_spec`；兼容读取修复后完整复跑
  `uv run pytest tests/test_workflow*.py -q` 最终为
  `945 passed, 8 skipped, 45 subtests passed`。
  另有核心确认/状态/bridge/renderer 集 `230 passed, 2 subtests passed`。
- 改动文件 Ruff 和 `git diff --check` 通过。仅出现 B054 已登记的两条固定版本 Lark SDK
  Python 3.13 deprecation warning，未使用过滤器掩盖。

### 后续风险

- 本轮没有迁移 Workflow IR，也没有改写无关卡片布局；尚未在真实飞书租户用不同
  provider/model 组合执行长任务和 Reviewer 故障注入，发布前仍需按计划做真实租户
  trace/report 对账。
- 提交为 `ffeb2e82`。

## Foundation Task 3 入站默认拒绝复审收口

### 变更

- 将 `message_id/chat_id/sender open_id/chat_type` 提升为完整请求信任根；仅接受
  `om_*/oc_*/ou_*` 与精确 `p2p|group`。纯策略、WebSocket intake、scheduler worker
  和管理员持久化服务逐层重复校验，shadow/legacy 也不能绕过畸形事件拒绝。
- WebSocket intake 在正文/图片解析前拒绝畸形事件；worker 不再复用入队时 decision
  或 TaskSpec 身份，而是从当前 event 重新解析命令并使用当前策略快照授权。拒绝路径
  不触达 scheduler、origin linker、项目上下文、群账本、图片下载、Shell 或 handler。
- `/setadmin` 的 `p2p_only` 删除无 request-context 兼容后门；`any_chat` 仍仅接受带
  规范 message/chat 的真实 P2P 或群事件。`/setadmin` 与 `/access allow-chat` 都把
  message_id 传到 service 做第二次上下文校验，管理员变更审计仅记录 sender/target hash。
- `AtomicEnvFileStore` 新增 flock 内 `update_with(mutator)` RMW 与完整不可变
  `EnvFileSnapshot`。rename 前失败、rename 后目录耐久性未知、已确认提交后的锁清理
  失败分别使用 `EnvPreReplaceError`、`EnvCommitUncertainError`、
  `EnvPostCommitCleanupError`；后两者携带可发布快照，避免声称磁盘或在线状态未变。
- rename 后异常从实际文件重新读取完整快照；service 按磁盘事实热切在线策略并记录
  结构化 BLOCKING finding。已确认提交但 lock close 失败单独记录
  `ingress_env_post_commit_cleanup_failed`，parent fsync 成功后的 directory close 失败也归入
  同一已确认提交清理态，不误报目录耐久性未知。下一次完整成功会清除运行时 finding；
  无法 reconcile/刷新时返回独立错误码和准确 UI 文案。
- dotenv 快照使用项目依赖的 `python-dotenv.parser.parse_stream`，兼容 `export`、单双
  引号和行尾注释且不插值；临时文件在 replace 前即为 0600，删除 replace 后 chmod
  窗口。allow-chat 从锁内磁盘集合 union，并只用锁内返回的完整 state 构造策略，多个
  进程并发授权不同群不会丢更新。
- `/status` 每次动态展示 `evaluate_security_posture()` 的 shadow、legacy、any_chat
  警告，以及共享 `IngressAccessPolicyProvider.blocking_findings`，使提交不确定、锁清理
  或在线刷新失败对管理员可见。
- runtime settings 镜像改为在 provider swap 前完成；任一字段失败会回滚已写字段、保持
  provider 旧快照，记录 `ingress_settings_mirror_failed` BLOCKING finding 并返回明确
  `settings_mirror_failed`，不再把 provider/settings 漂移说成全局成功。provider swap
  失败同样回滚 settings，回滚自身失败也会留下镜像阻断项。

### TDD 与验证

- 先新增复审回归并得到稳定红灯：首轮 `12 failed`，直接证明旧 request 未绑定
  message_id；后续分别锁定畸形事件解析副作用、TaskSpec 身份复用、原始 ID 审计、
  status 缺 finding、rename 阶段混淆、quoted dotenv 与并发 lost update。
- 专项复审文件最终 `93 passed`，含 spawn 8 进程的真实 flock allow-chat union、
  parent fsync 失败后的 actual-file reconciliation、lock_fd close 后 confirmed-commit
  cleanup 分类、parent fsync 成功后 directory close 的精确 fault injection、settings 镜像
  回滚、quoted/export dotenv 与 provider BLOCKING finding。
- 最终合并回归覆盖 ingress、admin、WS routing/worker、handler、diagnostics、config 和
  security posture：`593 passed`。相关改动文件 Ruff 与 `git diff --check` 全通过；仅有
  B054 已登记的两条固定 Lark SDK Python 3.13 deprecation warning。

### 后续风险

- 未发现遗留正确性失败；真实飞书租户仍应按发布清单复演首次 P2P 引导、群授权和
  目录 fsync 故障注入，确认运维卡片与本地确定性测试一致。
- 最终独立复审因运行时收尾时限中止；根代理完成补丁检查及跨三个保护门的
  `135 passed` 合并回归。提交为 `b3e30b4f`。

## Task 0.5 Worktree 终态、硬超时与真实评审

### 变更

- Dispatcher 改为显式管理 `ThreadPoolExecutor`；pool deadline 到达后不进入隐式
  `shutdown(wait=True)`，而是对每个仍运行 generation 设置取消栅栏、调用已绑定
  session 的 `cancel()`、只等待共享 `cancel_grace`，随后
  `shutdown(wait=False, cancel_futures=True)`。迟到 worker 由 generation 和 terminal
  lock 拒绝覆盖首个真实终态。
- 取消合同拆成 `cancellation_requested` 与 `cancellation_acknowledged`。只有 ACP
  transport future 在 timeout 内完成或后端返回显式确认才记 acknowledged；legacy
  CLI 的 `None`、ACP ack timeout、未绑定 session 和未知结果都不伪报确认。
- ACP prompt 终态统一经 fail-closed classifier：只有 `end_turn` 且无 pending plan、
  无非终态 tool call 才能完成；cancelled 保留为取消，空/unknown/timeout/max_tokens
  等停止原因进入失败。
- Manager 只有在全部必要 unit 完成且独立评审通过时才进入 `COMPLETED`；失败、取消、
  未知状态或评审非 PASS 都阻断 retry 后的高层成功、merge-ready 和自动合并。
  `merge_to_base()` 另有同样的 defense-in-depth gate。
- Review adapter 读取 working、committed 和未跟踪子目录的真实 diff；每个结构化 test
  必须与 dispatcher 或 review session 的结构化 execute command 匹配，且以同命令
  最新一次记录的 `exit_code == 0` 为准。命令不匹配/未知 exit code 为 INCONCLUSIVE，
  最新匹配非零为 FAIL；空 findings、空测试输出、未知 stop reason、无证据 blocker、
  解析失败全部默认拒绝。
- Worktree merge note 和实际卡片均明确披露现有 `worktree_branch_wins` 规则：发生同文件
  冲突时自动优先采用 Worktree 分支；未完成单元显示 blocked。

### 验证

- TDD 红灯先后复现：计划测试因缺少结构化 verdict/取消字段无法收集；stop reason
  映射 `6 failed`；review fail-closed 补测 `4 failed`；ACP ack 合同 `2 failed`、
  legacy CLI 未区分 requested/ack `1 failed`；伪造测试 PASS provenance `3 failed`；
  同命令先通过后失败仍误判通过 `1 failed`。
- 计划指定套件：
  `tests/test_worktree_dispatcher_timeout.py tests/test_worktree_terminal_truth.py tests/test_worktree_review_contract.py`
  为 `26 passed`。
- Task 18 与 manager/reporter/dispatcher 相邻套件为 `91 passed`；完整
  `tests/test_worktree_*.py tests/test_dispatcher_cancel_unit.py` 在隔离环境外为
  `347 passed`。
- ACP/RateLimit/Claude CLI/TTADK CLI 取消相邻回归为 `211 passed`；全部涉及文件 Ruff
  和 `git diff --check` 通过。仅保留固定 Lark 依赖的两条既有 deprecation warning。

### 范围

- Task 0.5 的保护门已完成；Foundation F18 仍因共享 F17 生命周期和 B8
  Worktree black-box orchestration node 未实施而保持 partial，本轮未伪装为完成。
- 提交为 `9054d221`；与本轮 F3、0.4 保护门的合并专项回归为 `135 passed`。

## 普通编程长任务卡片容量续接闭环

### 变更

- 将卡片 reducer 拆成共享的无裁剪投影与保底裁剪两层；普通编程适配器在事件会令
  总 block 超过 100 或 completed tool 超过 50 之前触发容量续接，其他卡片调用者仍保留
  原来的 100/50 防御性窗口。
- 容量续接先创建并投递新飞书卡，确认 `message_id` 可见后才切换当前 session 并归档
  旧卡。适配器级 `RLock` 串行化 ACP、flush、图片、计划、ticker 与终态事件；旧卡归档
  成功后冻结，后续事件只能进入新卡。容量路径不受语义轮转次数上限影响，且归档不再
  追加会触发二次裁剪的提示 block。
- 续接卡重放最新任务列表和有界 active-tool 快照；完整 active-tool registry 独立于当前页，
  即使并发 active tools 超过 100，被省略工具的迟到 delta/done/failed 也会先在当前页物化，
  不会丢事件或修改已冻结旧卡。
- 增加独立、只追加、按到达顺序记录的 main-text transcript。主 Agent 的 `on_text` 与
  ACP text chunk 在入站时记录，注册子代理文本排除；最终响应、超时/异常文本兜底和项目
  上下文不再读取可能被压缩的 `CardState`。
- 续接卡不可见或旧卡归档失败时立即设置 delivery failure fence：不把越界事件送入旧
  reducer，不再宣称卡片终态成功，由现有完整文本回复兜底。没有增加生产重启恢复，未
  创建 worktree，也未重启当前服务。
- 终态资源一并收口：成功归档、普通终态与 terminal retry 都永久关闭 timer manager，
  阻止 stale prewarning 在 cleanup 后重新挂载 TTL；锁竞争使用 3 次有界重排，持续争用
  后失败关闭并通知。`CardDelivery.close()` 即使内部 cleanup 抛错也释放 session lock，
  显式释放失败时保留 weakref finalizer 兜底。
- 全量门禁同时修正三处 leaf-lock 注释，并移除 WebSocket intake 中会把 slash parser
  编程错误静默降级为非命令的宽泛捕获；均为当前 `dev` 已有静态合同暴露出的最小修复。

### TDD 与验证

- 容量、transcript、归档顺序、失败 fence 与跨页 active-tool 首轮回归稳定红灯；复审
  追加的终态资源测试分别复现 archived timer 保留、retry 状态锁竞争、close 异常锁泄漏、
  stale defer 重建 timer 与永久 fence 缺失，修复后对应 focused 集全部转绿。
- 普通编程、卡片、timer、delivery、force-close、handler 与 reducer 扩展回归最终为
  `800 passed`；WebSocket/准入/锁序专项为 `274 passed`。
- 完整非慢速门禁最终为
  `12935 passed, 9 skipped, 118 deselected, 45 subtests passed`。首次全量暴露的两项静态
  合同失败已查明、修复并从头复跑为零失败，没有跳过、过滤或降低断言。
- 所有涉及 Python 文件 Ruff 通过；`uv run python -m src.main --validate` 配置校验通过；
  `scripts/test_inventory.py tests/` 以 0 退出；`git diff --check` 通过。仅出现固定 Lark SDK
  的两条 Python 3.13 deprecation warning。

### 后续风险

- 不包含进程重启后的续接恢复，符合本次范围；同步与异步投递、50/51 边界、100 block
  边界、两次以上容量轮转、超过 100 个 active tools、投递/归档失败及终态 timer 交错均有
  确定性回归。最终三轮独立复审均给出 READY，未留下已知正确性或资源生命周期问题。
